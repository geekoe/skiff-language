[package]
name = "skiff-artifact-identity"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
hex = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
skiff-artifact-model = { path = "../artifact-model" }
thiserror = "2.0"
