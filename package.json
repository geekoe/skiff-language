{
  "name": "skiff",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "skiff": "scripts/skiff.mjs"
  },
  "scripts": {
    "skiff": "node scripts/skiff.mjs",
    "type-check": "node scripts/verify.mjs --only type-check",
    "test": "node scripts/verify.mjs --only node",
    "verify": "node scripts/verify.mjs"
  }
}
