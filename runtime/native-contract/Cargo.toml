[package]
name = "skiff-runtime-native-contract"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
skiff-artifact-model = { path = "../../artifact-model" }
skiff-runtime-model = { path = "../model" }
skiff-trusted-registry-contract = { path = "../../trusted-registry-contract" }
