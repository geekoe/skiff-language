[package]
name = "skiff-runtime-linked-bytecode"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
skiff-artifact-model = { path = "../../artifact-model" }

[dev-dependencies]
serde_json = "1.0"

[lints]
workspace = true
