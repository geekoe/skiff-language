[package]
name = "skiff-runtime-deployment-image"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
skiff-artifact-model = { path = "../../artifact-model" }

[lints]
workspace = true
