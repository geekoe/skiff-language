import std

function run(seed: number) -> number {
  std.http.sse(std.http.HttpClientRequest {
    method: "GET",
    url: "http://phase5.test/unsupported-sse",
    headers: Array.empty<std.http.HttpHeader>(),
    body: null,
    timeoutMs: 5000,
  })
  return seed
}
