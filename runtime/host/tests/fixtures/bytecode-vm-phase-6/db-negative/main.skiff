type Doc {
  id: string,
  value: string,
}

db object Doc {
  primary key(id)
}

function run(seed: number) -> number {
  db transaction {
    db insert Doc { id = "nested" value = "bad" }
    db transaction {
      db delete Doc("nested")
    }
  }
  return seed
}
