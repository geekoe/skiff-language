db object Doc {
  primary key(id)
  value: string,
}

function work(value: number) -> void {
  return
}

function run(seed: number) -> number {
  db transaction {
    dispatch work(seed)
    db insert Doc { id = "bad" value = "task-negative" }
  }
  return seed
}
