type HandlerImpl implements payments.Handler {
  offset: number,
}

impl HandlerImpl {
  function handle(value: number) -> number {
    return value + self.offset
  }
}

function run(seed: number) -> number {
  final handler = HandlerImpl { offset: 1 } as payments.Handler
  return payments/invoke(handler, seed)
}
