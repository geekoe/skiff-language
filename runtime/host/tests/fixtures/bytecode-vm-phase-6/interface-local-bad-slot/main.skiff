interface Reader {
  function first(self: Self) -> string
}

type Impl implements Reader {
  value: string,
}

impl Impl {
  function first() -> string {
    return self.value
  }
}

function run(seed: number) -> number {
  final reader = Impl { value: "bad-slot" } as Reader
  return reader.missing().length()
}
