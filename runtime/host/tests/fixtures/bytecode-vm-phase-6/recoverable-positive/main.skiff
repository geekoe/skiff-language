type RecoverableDoc {
  id: string,
  value: string,
}

db object RecoverableDoc {
  primary key(id)
}

function run(seed: number) -> number {
  db transaction {
    db insert RecoverableDoc { id = "recoverable-codec" value = "ok" }
  }
  return seed
}
