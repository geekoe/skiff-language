type Doc {
  id: string,
  value: string,
}

db object Doc {
  primary key(id)
}

function run(seed: number) -> number {
  db transaction {
    db insert Doc { id = "phase6-db-commit" value = "ok" }
  }
  return seed
}
