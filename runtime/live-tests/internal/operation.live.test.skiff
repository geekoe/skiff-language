test defaultRun false

test "live operation dispatch executes runtime-owned fixture" {
  let marker = root.internal.operation.marker()
  assert root.internal.operation.echo(marker) == "runtime-live:".concat(marker)
}
