type DecodeError {
  target: string,
  message: string,
}

type ConflictError {
  target: string,
  message: string,
  retryable: boolean,
}

type ConstraintError {
  kind: string,
  packageId: string,
  collection: string,
}
