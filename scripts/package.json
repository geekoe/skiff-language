{
  "name": "@skiff/scripts",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "skiff": "skiff.mjs"
  },
  "scripts": {
    "skiff": "node skiff.mjs",
    "local-ingress": "node local-ingress.mjs",
    "dev-sync": "node skiff-dev-sync.mjs",
    "dev-sync:check": "node skiff-dev-sync.mjs --check --root ../compiler/tests/fixtures/router-websocket-fixture",
    "dev-sync:check-sync": "node verify.mjs --only scripts-dev-sync",
    "type-check": "node verify.mjs --only scripts-syntax",
    "test": "node verify.mjs --only scripts",
    "websocket-fixture:smoke": "node ../runtime/scripts/websocket-fixture-smoke-playwright.mjs"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
