[package]
name = "skiff-trusted-registry-contract"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
skiff-artifact-model = { path = "../artifact-model" }

[dev-dependencies]
serde_json = "1.0"
