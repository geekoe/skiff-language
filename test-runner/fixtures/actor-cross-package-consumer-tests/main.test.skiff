import std
import subjectImpl

test "top-level alias resolves provider actor for registry get and method calls" {
  const actor = std.actor.get<subjectImpl/thread_actor.ThreadActor>("thread-1")
  assert actor.read() == ""
  assert actor.setLabel("hello") == "hello"
  assert actor.read() == "hello"
}
