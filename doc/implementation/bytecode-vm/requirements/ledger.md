# Bytecode VM Requirement Ledger

Scope: 三个 included commits（`46351f72`、`576bbd31`、`430e5ff2`）对 `doc/architecture/` 与
`doc/reference/` 的全部语义 hunk。权威语义以 `430e5ff2` 的最终文本为准（README §2.2）。
Schema / ID / 状态 / owner / 维护规则见 [README.md](README.md)。

状态值：`missing`（默认）、`existing-needs-proof`、`implemented`、`retirement-only`。
本阶段（Phase 0）不做代码审计，所有行为要求默认 `missing`；仅当 included 文档明确断言当前
代码路径存在时标 `existing-needs-proof` 并在证据列注明文档断言位置。证据列统一以
`TBD（Phase 0 审计）` 开头，表示最终证据待审计补齐。

---

## A. `46351f72` — docs(architecture): redesign bytecode VM runtime model

新增 `doc/architecture/bytecode-vm.md`（877 行，16 个小节）。语义要求按小节归并。

| ID | Commit | 文件 | 语义要求 | Owner | 状态 | 目标阶段 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R-001 | 46351f72 | doc/architecture/bytecode-vm.md | 全部 service/package 代码经一个扁平同步 bytecode dispatch loop 执行；普通 local call 不创建 Rust future / async closure / 原生递归 evaluator frame。 | `runtime/vm`（新 crate，README §6.4） | missing | Phase 4 | TBD（Phase 0 审计） |
| R-002 | 46351f72 | doc/architecture/bytecode-vm.md | VM 只在一个 pending-capable effect 本次实际返回 `Pending` 时停放 fiber；transitive `mayPending` 不触发预先挂起，也不产生另一套 local call ABI。 | `runtime/vm` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-003 | 46351f72 | doc/architecture/bytecode-vm.md | artifact 保存 relocatable ISA；runtime 只执行已完成 link 且通过 verifier 的 `LinkedBytecodeImage`；production 不保留旧 artifact reader、双解释器或按版本 fallback。 | `artifact-model/src/bytecode.rs`（倾向）＋ `runtime/loader/src/runtime_assembly.rs` | missing | Phase 1 / 3B / 8 | TBD（Phase 0 审计） |
| R-004 | 46351f72 | doc/architecture/bytecode-vm.md | VM slots / operand stack / collection 元素使用紧凑固定宽度 value slot；nominal / catch / union identity 必须保留但不逐值复制完整 identity object。 | `runtime/model/src/value.rs` | missing | Phase 4 / 6B | TBD（Phase 0 审计） |
| R-005 | 46351f72 | doc/architecture/bytecode-vm.md | VM frame/value stack 与语言 heap 分离；call return 回收 frame segment；eligible tail call 原地替换 frame，空间不随 tail hop 增长。 | `runtime/vm` | missing | Phase 4 | TBD（Phase 0 审计） |
| R-006 | 46351f72 | doc/architecture/bytecode-vm.md | 每 request 独立 GC-capable managed heap；是否运行 collector 只由 allocation pressure 决定，不由请求时长或入口类型决定。 | `runtime/model/src/request_heap.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-007 | 46351f72 | doc/architecture/bytecode-vm.md | 同一 request 可有多 runnable/waiting fiber 与真实重叠外部 I/O，但任意时刻只有一个 fiber 执行 bytecode 或修改 request heap（cooperative single-owner）。 | `runtime/vm` ＋ `runtime/request/src/runner.rs` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-008 | 46351f72 | doc/architecture/bytecode-vm.md | sync/ready 热路径、execution budget、profiling statement units、source attribution、exception identity、timeout/cancellation、Actor ownership、recoverable boundary 语义不因 VM 迁移改变。 | `runtime/request/src/execution_budget.rs` 等（§6.5） | missing | Phase 8 / 9 | TBD（Phase 0 审计） |
| R-009 | 46351f72 | doc/architecture/bytecode-vm.md | artifact ISA 不暴露 Rust pointer、allocator address、`Vec` layout、native future layout 或 runtime quickening 细节；为后续 JIT 保留显式类型/CFG/stack-effect/safepoint 信息。 | `artifact-model/src/bytecode.rs` | missing | Phase 1 | TBD（Phase 0 审计） |
| R-010 | 46351f72 | doc/architecture/bytecode-vm.md | Source effect analysis 是 callable transitive may-effect 唯一 owner；lowering 不得再次遍历 AST 推断另一份 `maySuspend`。 | `compiler/source/src/callable_effects.rs` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-011 | 46351f72 | doc/architecture/bytecode-vm.md | Bytecode emitter 负责控制流、stack effects、constant pool、relocation、frame layout、exception region、statement entry 与 source map，不制造 assembly-local exact address。 | `compiler/emission/src/lib.rs` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-012 | 46351f72 | doc/architecture/bytecode-vm.md | Assembly linker 消费 exact package/deployment/image facts，把 relocation 解析为 image-local target、type tag、shape、effect adapter 与 capability table entry。 | `runtime/linker/src/linker.rs` | missing | Phase 3B | TBD（Phase 0 审计） |
| R-013 | 46351f72 | doc/architecture/bytecode-vm.md | Admission verifier 独立验证 linked code；compiler/linker 产出的 max stack、target kind、effect 声明不能直接视为可信。 | `runtime/linker`（verifier 模块） | missing | Phase 3B | TBD（Phase 0 审计） |
| R-014 | 46351f72 | doc/architecture/bytecode-vm.md | VM core 只拥有 program frame、value stack、控制流、unwind 与 effect invocation protocol；不直接实现 DB/HTTP/service routing/Actor admission/native resource lifetime。 | `runtime/vm` | missing | Phase 4 | TBD（Phase 0 审计） |
| R-015 | 46351f72 | doc/architecture/bytecode-vm.md | Request scheduler/effect adapters 拥有 pending operation、wake、cancel、deadline、fiber queue 与 host I/O；adapter 不得保存可被 GC 移动的裸 heap pointer。 | `runtime/request/src/runner.rs` ＋ `runtime/native/src/` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-016 | 46351f72 | doc/architecture/bytecode-vm.md | Canonical projection 与所有 test fixture 进入同一个 emitter/linker/verifier/VM 形状；测试可用内存 artifact store，但不能有 test-only opcode、第二套 evaluator 或 assembly 失败后的 legacy fallback。 | `test-runner` ＋ `runtime/loader/src/deployment.rs` | missing | Phase 8 | TBD（Phase 0 审计） |
| R-017 | 46351f72 | doc/architecture/bytecode-vm.md | ISA 定义：opcode/operand schema、每指令 operand-stack 输入输出、branch/call/tail call/return/throw/effect invoke 语义、constant/type/shape/target/relocation index 含义、verifier 规则、ISA/schema version。 | `artifact-model/src/bytecode.rs` | missing | Phase 1 | TBD（Phase 0 审计） |
| R-018 | 46351f72 | doc/architecture/bytecode-vm.md | Wordcode 编码：`u32` word sequence、固定 operand word 数、operand 只保存 immediate/relative branch/slot/pool/relocation index、jump target 必须指向 instruction header。 | `artifact-model/src/bytecode.rs` | missing | Phase 1 | TBD（Phase 0 审计） |
| R-019 | 46351f72 | doc/architecture/bytecode-vm.md | `RelocatableBytecodeFunction` 携带 functionKey、words、constants、relocations、frameLayout、maxOperandDepth、exceptionRegions、statementEntries、sourceMap；debug binding name 进独立 debug table 不扩大 runtime frame。 | `artifact-model/src/bytecode.rs` | missing | Phase 1 | TBD（Phase 0 审计） |
| R-020 | 46351f72 | doc/architecture/bytecode-vm.md | Relocation 语义：Package artifact 不保存最终 `ExecutableAddr`；relocation 种类至少含 LocalExecutableRef/PackageCallableRef/InterfaceMethodRef/TypeRef/ShapeRef/EffectRef/ConstantRef；linker 生成 immutable image-local table；共享代码不因不同 assembly 被原地 patch。 | `artifact-model/src/bytecode.rs` ＋ `runtime/linker/src/linker.rs` | missing | Phase 1 / 3B | TBD（Phase 0 审计） |
| R-021 | 46351f72 | doc/architecture/bytecode-vm.md | Decoded micro-ops：admission 后可解码为固定 Rust micro-op array 并 quickening；superinstruction 只允许存在于 decoded/JIT 层，不得进入 artifact schema，不得改变 semantic charging / statement profiling / source attribution。 | `runtime/vm` | missing | Phase 4 / 9 | TBD（Phase 0 审计） |
| R-022 | 46351f72 | doc/architecture/bytecode-vm.md | 首版 ISA 至少包含所列语义族（Value/slot、Control、Call、Record/value、Collection、Exception/control region、Effect）；numeric opcode 由 artifact schema 单一声明生成，compiler 与 runtime 不得各自复制编号。 | `artifact-model/src/bytecode.rs` | missing | Phase 1 | TBD（Phase 0 审计） |
| R-023 | 46351f72 | doc/architecture/bytecode-vm.md | `copy_slot`/`move_slot` 是物理 ownership/share facts；verifier 必须证明 `move_slot` 后源 slot 不再被读取；不存在 transitive-function `call_suspend`，不存在无语言语义 owner 的 `yield`/`make_closure`。 | `runtime/linker`（verifier）＋ `artifact-model` | missing | Phase 1 / 3B | TBD（Phase 0 审计） |
| R-024 | 46351f72 | doc/architecture/bytecode-vm.md | Admission verification 至少检查：opcode/version 已知、index 界内、relocation 已解析为允许的 exact target kind、pc 指向 instruction header、CFG 无 stack underflow 且 merge point 一致、max depth 不超声明、typed stack 与 type/shape plan 相容、exception region 嵌套合法、pending-capable instruction 有唯一 resume descriptor、`NoPending` 函数不可达 pending-capable effect、tail call 资格、move/builder/writable path 无 use-after-move/同时可写 alias、statement/source 表有序完整、size 不溢出；unknown fact 必须 fail closed 或保守降级。 | `runtime/linker`（verifier） | missing | Phase 3B | TBD（Phase 0 审计） |
| R-025 | 46351f72 | doc/architecture/bytecode-vm.md | `CallableEffectSummary` 保留并成为唯一 owner，至少表达 writesCallerReachable/returnsCallerAlias/throwsCallerAlias/escapesCallerValue/requiresSameHeapIdentity/invokesUnknownTarget/mayPending；`maySuspend` 迁移时删除，由 `mayPending` 取代；SCC fixed point；interface/service/callback/未知动态 target 保守含 Unknown pending effect；pending-effect categories 可保存。 | `compiler/source/src/callable_effects.rs` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-026 | 46351f72 | doc/architecture/bytecode-vm.md | `mayPending` 用于 const/pure 验证、sync-only boundary、caller-writable callee 的 `NoPending`、concurrent lane 检查、effect guarantee、JIT sync-only region、verifier 交叉检查；不用于选择 local call 形态、不分配 continuation、不决定 Ready/Pending、不阻止 tail call；lowering 的独立 `suspend_analysis` 迁移完成后删除。 | `compiler/source/src/callable_effects.rs` ＋ `compiler/lowering/src/suspend_analysis.rs` | missing | Phase 2 / 8 | TBD（Phase 0 审计） |
| R-027 | 46351f72 | doc/architecture/bytecode-vm.md | 固定宽度 `ValueSlot`（首版目标 16B，runtime layout 非 artifact ABI）；immediate（null/bool/number/integer/date）与 reference（RequestHeapRef/ActorStateRef/ConstRef/ResourceRef）区分；非小型 payload 不 inline 到 Rust enum。 | `runtime/model/src/value.rs` | missing | Phase 4 / 6B | TBD（Phase 0 审计） |
| R-028 | 46351f72 | doc/architecture/bytecode-vm.md | Nominal/catch identity：`compactTypeTag`/`TypeTagId` 精确区分 nominal record/representation、named-union 上下文与分支、catch leaf、interface payload concrete identity；image 保存 TypeTagId→完整 identity/type plan；throw/catch/boundary/recoverable encode 经表展开，不从 runtime shape 猜测。 | `runtime/model/src/type_plan.rs` | missing | Phase 4 / 6B | TBD（Phase 0 审计） |
| R-029 | 46351f72 | doc/architecture/bytecode-vm.md | Handle discipline：heap 引用用 stable table index 并含 heap-space/generation validation，不保存裸 object pointer；collector 移动只更新 handle entry；request handle 不得进入另一 request/artifact/durable record/boundary payload；Actor/constant/resource reference 是不同 kind 不能伪装。 | `runtime/model/src/request_heap.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-030 | 46351f72 | doc/architecture/bytecode-vm.md | 内存分成 owner：LinkedBytecodeImage/ConstantHeap（immutable 跨 request 共享）、VmFiber（frames/slots/operand stack/regions）、RequestManagedHeap（request-local）、ActorStateHeap（instance-local versioned）、ResourceTable（显式 close/cancel/drop）；VM stack 用 request-owned contiguous segments 纳入统一 budget；resource 的 close/cancel 不能依赖 tracing collector 的不确定时机。 | `runtime/model` ＋ `runtime/vm` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-031 | 46351f72 | doc/architecture/bytecode-vm.md | Collection/record 表示：value semantics（snapshot，writable `InOut` 模式由 reference 定义）；Array SmallInline/SmallFlat/UniqueFlat/SharedFlat、Map SmallLinear/UniqueHash/SharedHash，可选 persistent 形式只在 benchmark 证明需要时引入；unique edit token（move 保留、copy/escape/freeze/share 转 shared、COW）；transient builders/freeze；dense record 用 ShapeId+dense slots（verified offset）；动态 JSON/Map 用独立 hash rep；string/bytes immutable（chunk/rope/unique builder，避免 O(n²)）。 | `runtime/model/src/value.rs` ＋ `compiler/lowering` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-032 | 46351f72 | doc/architecture/bytecode-vm.md | Request-local GC：每 request 同一 GC-capable heap（handle table/bump nursery/mature pages/large-object）；无 short/long request 类别；精确 roots（fiber slots/exception regions/PendingOperation roots/request context/transaction-Actor overlays/resource payload roots）；safepoint 位置（allocation slow path、effect enter/return、backedge budget poll、scheduler handoff）；collector 只在无 fiber 修改 heap 且所有 pending owner 已登记 roots 时运行。 | `runtime/model/src/request_heap.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-033 | 46351f72 | doc/architecture/bytecode-vm.md | 无 raw borrow across allocation/await：adapter 只能在无分配 instruction scope 内短暂 borrow；Pending operation 只保存 stable handle+registered root 或已 materialize 的 owned bytes/host value；违反是 boundary bug，不能用 pin-all 规避。 | `runtime/boundary/src/` ＋ `runtime/native/src/` | missing | Phase 5 / 6B | TBD（Phase 0 审计） |
| R-034 | 46351f72 | doc/architecture/bytecode-vm.md | Unified memory budget 统计实际 reserved/owned capacity（heap pages、VM stack pages、pending buffers、collection/string capacities、transaction journal、resource estimates、Actor overlay）；soft limit 先收集，hard limit 产生结构化 memory resource error；request terminal 释放全部 request 资源。 | `runtime/request/src/runner.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-035 | 46351f72 | doc/architecture/bytecode-vm.md | Fiber/frame 布局：VmFiber（state/currentFunction/pc/frames/values/regions）、Frame（functionIndex/returnFunction/returnPc/slotBase/slotCount/operandBase/operandDepth/callSite/instantiatedTypeContext）；进入 frame 一次扩展 verified max depth segment；return 截断并移结果；non-tail recursion 不增长 native stack 但受统一 budget 约束，耗尽返回结构化 resource error。 | `runtime/vm` | missing | Phase 4 | TBD（Phase 0 审计） |
| R-036 | 46351f72 | doc/architecture/bytecode-vm.md | Local call：`call_local` 按源码顺序求值参数，以 borrow/move/share 准备 slots，同一 dispatch loop 切到 callee，不检查 callee transitive `mayPending` 不创建 future；深处实际 Pending 时整个 fiber 一起停放。 | `runtime/vm` | missing | Phase 4 | TBD（Phase 0 审计） |
| R-037 | 46351f72 | doc/architecture/bytecode-vm.md | Tail call：eligible exact-local 发射 `tail_call_local`；return plan/nominal tag/generic/self/cleanup eligibility 遵守 tail-call-execution.md 用户语义；替换 frame 不 push；每 hop 保留 budget charging；eliminated edge 不增长诊断栈；callee 后续 effect Pending 不影响 eligibility。 | `runtime/vm` ＋ `compiler/emission/src/lib.rs` | missing | Phase 4 | TBD（Phase 0 审计） |
| R-038 | 46351f72 | doc/architecture/bytecode-vm.md | 显式 caller-writable 参数（`InOut`，若 reference 引入）：argument 必须是 writable `var` access path、调用期 exclusive、callee verified `NoPending`、unique 原地写/shared 时 COW、call boundary 不复制、throw writeback 语义由 reference 定义（VM 以显式 writable region 实现）；loan 跨 Pending 需新 reference/region contract。 | `compiler/source` ＋ `runtime/vm` | missing | Phase 2 / 4 | TBD（Phase 0 审计） |
| R-039 | 46351f72 | doc/architecture/bytecode-vm.md | Actual-Pending suspension：只有 leaf effect instruction（invoke_service/actor/http/db/stream_next/emit/timer/file/callback/unknown）可停放 fiber；统一为 typed `invoke_effect`+linked adapter descriptor；pure native/collection/local-call 不能返回 Pending；EffectStart=Ready|Pending。 | `runtime/vm` ＋ `runtime/native/src/registry.rs` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-040 | 46351f72 | doc/architecture/bytecode-vm.md | Park 协议：invoke_effect 前 verifier 已知 stack input/result/source/resume descriptor；Pending 时记录 {pendingId, resumePc, expectedStackHeight, resultPlan, sourceSite}，fiber 移入 waiting table，scheduler 拥有 PendingOperation/deadline/cancel/wake；挂起是 fiber 所有权移动，不是每层 heap continuation。 | `runtime/vm` ＋ `runtime/request/src/runner.rs` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-041 | 46351f72 | doc/architecture/bytecode-vm.md | Resume 协议：成功按 plan 导入并回 runnable；失败在原 effect source site 注入 error 走同步 throw 的 exception/cleanup unwind；Cancellation/internal stop 走 terminal unwind 不伪装成可捕获值；budget/profiling/type substitution/Actor frame/transaction region/diagnostic prefix 挂起前后不重置。 | `runtime/vm` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-042 | 46351f72 | doc/architecture/bytecode-vm.md | Cross-owner calls：exact local program call 始终 `call_local`；service/Actor/callback 是 effect boundary（encode/freeze→PendingOperation→provider activation→decode+resume）；provider 与 caller 不共享 raw request handle；same-process 优化也必须保持 owner/heap/error channel/generation boundary。 | `runtime/boundary/src/plan.rs` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-043 | 46351f72 | doc/architecture/bytecode-vm.md | Structured concurrency：`concurrent` 每 lane 独立 VmFiber，共享 request scheduler 与允许共享的 immutable roots；多个 pending I/O 真实重叠，heap mutation 串行；join/winner/cancel/deterministic error selection 属 structured-concurrency owner；lane terminal 不能自行结束 request 或跳过 sibling cancellation/cleanup。 | `runtime/vm` ＋ `compiler/lowering` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-044 | 46351f72 | doc/architecture/bytecode-vm.md | Exception table：ExceptionRegion（startPc/endPc/handlerPc/handlerStackHeight/catchMatcher/catchSlot/cleanupDepth）；catchMatcher 是 linked catch-leaf/type-tag matcher；handler 进入前截断 operand stack 并保留 correlation/source envelope；fiber active region stack（timeout/DB transaction/structured join/stream supervision/with-resource/Actor overlay）定义 normal/throw/cancel/internal-stop 的 cleanup/commit/rollback。 | `runtime/vm`（unwind） | missing | Phase 4 / 6A | TBD（Phase 0 审计） |
| R-045 | 46351f72 | doc/architecture/bytecode-vm.md | Transaction snapshot 不深 clone 整个 heap graph：value-semantic root checkpoint + edit epoch（checkpoint 可回滚 root、first write COW 或精确 mutation journal、rollback 恢复 checkpoint 后 GC 回收、commit 发布新 roots、nested 用嵌套 epoch/journal boundary）。 | `runtime/vm` ＋ `runtime/boundary` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-046 | 46351f72 | doc/architecture/bytecode-vm.md | Actor/durable/boundary memory：ActorStateHeap instance-local、versioned、可 compact；invocation 用 immutable snapshot/root + transactional edit overlay（entry 冻结 generation、commit 原子发布、rollback 丢弃 overlay、request-local resource 不能写进 Actor state、compaction 更新 handle/epoch）；boundary 保存 stable schema/code/type identity 与编码 payload，不保存 ValueSlot/request handle/GC generation/pc/pending id。 | `runtime/eval/src/actor_instance.rs` ＋ `runtime/boundary` | missing | Phase 7 / 6B | TBD（Phase 0 审计） |
| R-047 | 46351f72 | doc/architecture/bytecode-vm.md | Execution budget/profiling/attribution：semantic instruction charging 与 deadline/cancel poll 是不同机制；emitter 为语义点生成稳定 charge metadata；VM 可批量提交；quickening/superinstruction/decoded 数/JIT 指令数不能改变 semantic units；迁移首版保持当前可观察 instruction-count/budget fixtures；statement entry 恰好记录一个 profiling unit；source map 覆盖 call/throw/effect/DB/timeout/generated instruction；挂起/恢复不重复/不漏记 statement；diagnostic stack 用 exact call site，cross-service 经 canonical error channel 投影。 | `runtime/vm` ＋ profiling 工具链 | missing | Phase 4 / 9 | TBD（Phase 0 审计） |
| R-048 | 46351f72 | doc/architecture/bytecode-vm.md | Performance contract：结构性保证清单（零 host heap allocation 的 sync local call、固定宽度 slot 操作、constant load 零 request allocation、verified offset field access、unique Array push O(1)/Map put expected O(1)、builder 不逐元素复制、低分配 request 零 GC cycle、Ready effect 不 park 不分配 continuation、Pending 只分配一个 pending owner、tail recursion O(1)、allocation-heavy request 可回收中间值、quickening 不扩大 ISA）。 | `runtime/vm` | missing | Phase 4 / 9 | TBD（Phase 0 审计） |
| R-049 | 46351f72 | doc/architecture/bytecode-vm.md | Release benchmark suite 至少覆盖：pure loop、deep sync calls/tail calls、dense record、unique Array/Map、COW mutation、JSON/DB materialization、string concat、Ready effect、Pending park/resume、long request allocation/collection、真实 Agine LLM SSE reducer/chat smoke；目标是 sync interpreter hot path 相对 tree evaluator 数量级改善（预期 5-20x），阈值必须绑定 workload/release profile/机器/统计口径/baseline commit；不能用 I/O latency 掩盖 interpreter regression。 | benchmark tooling（`runtime` 侧 harness） | missing | Phase 0（登记）/ 9（执行） | TBD（Phase 0 审计） |
| R-050 | 46351f72 | doc/architecture/bytecode-vm.md | Completion contract：reference 收敛（var/let/const、value semantics、mutating receiver/static transform）、effect analysis 唯一 owner、schema 单一声明、PackageArtifact 保存 relocatable bytecode、canonical 与 test fixture 共享同一链、heap owner 分离、focused test 覆盖清单、真实路径+GC pressure+Agine chat smoke 通过、tree evaluator/old reader/call_suspend/test-only evaluator/fallback 全部删除；instruction 变更必须同步更新 schema/emitter/linker/verifier/decoded runtime/mapping/tests；物理优化不升级 ISA，语义变化必须升级 ISA/schema version；迁移细节归 implementation 目录。 | 全局（所有 §6 区域） | missing | Phase 8 | TBD（Phase 0 审计） |

小计：50 行（1 文件，16 小节逐节覆盖）。

---

## B. `576bbd31` — docs(architecture): consolidate VM runtime contracts

40 个文件（31 M + 9 D）。`D` 文件为 retirement obligation，各一行；`M` 文件按语义单元归并。

| ID | Commit | 文件 | 语义要求 | Owner | 状态 | 目标阶段 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R-051 | 576bbd31 | doc/architecture/actor-instance-evaluator-design.md（D） | Retirement：canonical 字段表设计稿删除，被 actor-shared-heap-design.md 取代；其概念（per-field generation/dirty tracking、E3 bridge、per-lane heap 克隆、snapshot_persistent_fields/resume 导入、ActorInstanceExecutionLease fields+heap 克隆、with_transaction_live_fields 等）不得重新引入，对照代码审计后删除。 | `runtime/eval/src/actor_executor.rs`（删除面） | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-052 | 576bbd31 | doc/architecture/actor-model.md | Actor 面并发语义：成员方法的同步段在同一实例上串行执行，只有 actual `Pending` 释放 segment lease 后协程才交错；stream next/异步 service call/timer 等只是潜在 suspension point，实际等待才释放；静态 `maySuspend` 不产生预先释放；失败只结束调用不回滚本段已完成的 Actor 写入；无显式 `yield`、无自动抢占。 | `runtime/eval/src/actor_executor.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-053 | 576bbd31 | doc/architecture/actor-model.md | 每次 actor get/create 或方法调用携带 exact deployment `buildId`；Runtime 以该 buildId 为唯一加载键，取得或懒加载 immutable `DeploymentExecutionImage` 后才进入 Actor admission；invocation 及 continuation pin 该 execution owner，不从 ambient release pointer 重新选择代码/配置；不存在 `RuntimeAssembly`、current assembly 或 activation generation。 | `runtime/host/src/host/actor_owner_execution.rs` ＋ `runtime/loader/src/deployment.rs` | missing | Phase 3A / 7 | TBD（Phase 0 审计） |
| R-054 | 576bbd31 | doc/architecture/actor-model.md | `create` 是平台保留方法，只在平台为新 incarnation 执行的 Actor 激活路径调用；key 字段由平台写入；create 期间实例未 admission（挂起不产生半初始化可见性）；create 签名/definite assignment 规则保持。 | `runtime/eval/src/actor_instance.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-055 | 576bbd31 | doc/architecture/actor-model.md | `dispatch` actor-method target：提交时同时冻结 exact deployment `buildId` 与 `ActorActivationSnapshot`；Runtime 只从该 buildId 的 immutable image 执行目标方法；registry entry 丢失时用 snapshot 恢复最小 entry（put-if-absent）后 create+调用；dispatched 调用按同步段串行、actual Pending 后与其它调用交错；task 不保存 Actor 内存字段；task lease 不替代 actor owner lease。 | `router/src/task/actor_attempt.rs` ＋ `runtime/eval/src/actor_dispatch.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-056 | 576bbd31 | doc/architecture/actor-model.md | Registry entry 是 Actor 激活所需的最小易失事实，不是持久层也不是 deployment activation state；Router 重启/进程丢失/operator 清理都可能丢失；普通入口用 `get` 的 id/create 参数从业务事实重建，durable task 用冻结的 `ActorActivationSnapshot` 做同一 put-if-absent 恢复；两条路径都不恢复旧 Actor 内存字段。 | `router/src/actor/activation.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-057 | 576bbd31 | doc/architecture/actor-model.md | 每 live 实例拥有 instance-owned shared arena 与稳定 field root；同实例所有方法协程直接共享 state，不把字段克隆到逐方法 request heap，无 return-time commit overlay；每同步段持 `ActorSegmentLease` 直接读写 shared arena，写入立即可见，return/failure 不 commit 副本也不回滚已执行写入；恢复前重新 acquire 并校验 actor identity、exact buildId/image owner、implementation identity、incarnation fence、arena epoch，不匹配 fail closed；恢复后必须假设字段已变化。 | `runtime/eval/src/actor_instance.rs` ＋ `runtime/eval/src/actor_executor.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-058 | 576bbd31 | doc/architecture/actor-model.md | Actor 字段 value semantics：普通 record/Array/Map 赋值、普通传参、返回与 container store 产生 logical snapshot；局部 `let` 和普通参数不可写；局部 `var` 是 writable binding，首次写共享 backing 时 path COW 分离；把 `self.field` 读入普通 local 或传给普通参数只得到 snapshot 不获得 mutable alias；直接 writable `self.field` path 仍修改 Actor shared state。 | `runtime/eval` ＋ `runtime/model/src/value.rs` | missing | Phase 6B / 7 | TBD（Phase 0 审计） |
| R-059 | 576bbd31 | doc/architecture/actor-model.md | 任期与版本：actor logical identity 不含 service version 或 deployment `buildId`；任期钉死单一 owner runtime、implementation identity、incarnation epoch；每个 invocation/continuation 另 pin 自己的 exact buildId 与 immutable image；不同 build 但 implementation identity 相同可共用同一 live incarnation，但每次 invocation 必须用自身 pin 的 exact-build image；implementation identity 不同不允许两 incarnation 并发拥有同一 logical identity，不迁移旧实例内存字段。 | `router/src/actor/types.rs` ＋ `runtime/host/src/host/actor_owner_execution.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-060 | 576bbd31 | doc/architecture/actor-model.md | 升级协议（576bbd31 版；430e5ff2 后续改为拒绝模型）：upgrading 原子标记、关闭新调用 admission、旧方法运行到最近一次 actual `Pending` 或正常返回、恢复前重新 acquire segment lease 并检查 incarnation fence 与 arena epoch、active 清零后销毁旧实例推进 epoch 并在目标 exact buildId 的 runtime 上按 entry 创建输入执行 create；实现完全相同且 ABI 兼容的旧请求不继续处理。 | `router/src/actor/ownership.rs` ＋ `runtime/eval/src/actor_executor.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-061 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | v4 权威设计：每 actor 实例一个共享 arena、字段为稳定根 slot；方法段级借用只在 actual `Pending` 才释放 `ActorSegmentLease`，恢复时 reacquire（零复制零编码）；shared state live（挂起期间其它方法对 field root 的修改立即可见）；读 field 到普通 local 得到 O(1) snapshot 不获得 mutable alias；普通 request 保持 Exclusive heap 路径。 | `runtime/eval/src/actor_instance.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-062 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | v1 非目标：不做 canonical 字段表/字段版本化/惰性解码；Actor arena 无 tracing GC（quiescence 压缩+实例逐出）；不支持 `concurrent`/`serial`（编译期拒绝，删除运行时 concurrent 机制与 E3 bridge）；actor 方法支持 DB-only `db transaction`（禁 nested、禁 body 写 actor field）；跨 runtime 编解码不变；不做硬线程亲和（inline 执行模型）。 | `runtime/eval/src/actor_*` ＋ `compiler/lowering` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-063 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | 段生命周期：acquire 校验 Actor identity、exact buildId/image owner、implementation identity、incarnation fence、arena epoch 后取得 segment lease；同步段直接读写 shared arena（无副本无 wire 往返），request-scoped 校验在写路径执行；actual Pending 才释放 guard 与 lease；恢复 reacquire 并重新校验；return/failure 归还 lease，字段留在 arena，无 commit overlay，失败不回滚已执行写入。 | `runtime/eval/src/actor_executor.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-064 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | 删除项：`ActorInstanceExecutionLease` fields+heap 克隆、`snapshot_persistent_fields`/resume 导入、`write_field` wire 往返（保留类型校验与 request-scoped 校验）、`commit_execution` 字段+heap 发布（return/failure 只归还 lease）、`ActorExecutionFrame::suspend/resume` 改 drop/reacquire+fence 校验、`with_transaction_live_fields`/arena 事务回滚接入删除。 | `runtime/eval/src/actor_*` | retirement-only（实现迁移） | Phase 7 / 8 | TBD（Phase 0 审计） |
| R-065 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | Value snapshot/共享 state/失败段语义：direct `self.field` 读取总是当前 field root；普通赋值/传参/返回/container store 产生 logical snapshot；`let x = self.items` 与普通 parameter 是 immutable snapshot；`var x = self.items` 是 writable snapshot，后续 local mutation 经 path COW 分离不暗中改 `self.items`；field-to-field 赋值复制 logical value 不建可观察 mutable alias；失败段不保证字段原子性（throw/internal failure 保留已执行写入）。 | `runtime/eval/src/actor_executor.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-066 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | HeapAccess 双模式改造：future 实际返回 `Pending` 后保存的状态中不得有共享 arena 派生的 `&mut RequestHeap`、共享 guard 或仍持有的 `ActorSegmentLease`；heap 访问一律走 `HeapAccess`（Exclusive/Shared）；`EvalContext.heap` 改为 `HeapAccess`，`heap_mut()` 定义在 `HeapAccess` 上；漏斗化所有跨 await 载点（130+，含 DbIrEvaluator、timeout 子求值器、program stream、callback、deployment image/provider 分发）；guard 跨 await 存活由 debug 断言+纪律测试拦截。 | `runtime/eval/src/eval_context.rs` ＋ `runtime/eval/src/heap_access.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-067 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | DB-only transaction（v1）：actor 方法内 `db transaction` 的 commit/abort 只作用于 DB，不为 Actor arena 建立 snapshot overlay；abort 不恢复 DB 之外的 Actor 写入；transaction body field-write 禁令由 canonical effect summary 闭合同包、本地 helper、package-direct 调用；unknown/dynamic target 保守拒绝；nested 禁止。 | `compiler/source/src/callable_effects.rs` ＋ `runtime` | missing | Phase 6A / 7 | TBD（Phase 0 审计） |
| R-068 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | quiescence 压缩：实例维护 active/suspended 续体计数（含 create 与 transaction cleanup）与 upgrade/discard 状态；计数==0、无 pending upgrade/discard、arena 超阈值才触发；锁内克隆 live 根→bump arena epoch→原子替换；upgrade/discard 优先；per-instance arena limits 超限报平台错误。 | `runtime/eval/src/actor_executor.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-069 | 576bbd31 | doc/architecture/actor-shared-heap-design.md | 线程模型：v1 保持 driver inline poll 模型，不把 actor 段独立 `tokio::spawn` 到 worker 池；不把实例硬绑定固定 OS 线程；`ActorSegmentLease` 与 arena guard 保证任意时刻只有一个线程访问该实例 state；跨 runtime 边界：每次 invocation 携带 exact buildId，owner Runtime 按该 key 取得/懒加载 image，method entry/resume 校验 exact image owner 与 implementation identity；Actor shared arena 属 instance 不属 image。 | `runtime/eval/src/actor_executor.rs` ＋ `runtime/host` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-070 | 576bbd31 | doc/architecture/any-interface-value.md | 值语义、可写 place 与 `InOut`：ordinary record/object/Array/Map/JsonObject 与 `InterfaceCarrier::Local.payload` 采用 value semantics（赋值/传参/返回/container store/`as I` 装箱都产生逻辑 snapshot；move/shared backing/COW 可用但不得让 snapshot 写入被另一 snapshot 观察）；绑定可写性是静态 place fact（`let`/普通 parameter/loop/pattern/with binding 派生 path 不可写；`var` 派生精确 path 可写；顶层 `const` 是 compiler-evaluated、request-independent、deeply frozen 值）；`inout` 是 caller-writable exclusive loan，必须满足：var 派生精确 exclusive place + 实参重复 `inout`、exact Package-local/package-direct concrete target 且进入 Package Local ABI、compiler 与 verifier 都证明 `NoPending`；`inout` 不得出现在 interface requirement/method table/callback/ServiceContract/gateway/Actor external/host effect/recoverable payload；verifier 拒绝任何绕过尝试。 | `compiler/source` ＋ `runtime/linker`（verifier） | missing | Phase 2 / 3B / 6A | TBD（Phase 0 审计） |
| R-071 | 576bbd31 | doc/architecture/any-interface-value.md | Execution owner：每次 request、stream producer 或 callback invocation 先 pin exact deployment `buildId`，只在该 build 的 immutable image 内解析 executable/method table/type-restore plan/ConstantHeap；`InterfaceValue` 不重复保存 buildId，Local 分支 linked id 由当前 execution frame 的 exact image 定界；`Remote` carrier 保存坐标，每次进入 provider 时解析并 pin 当次 exact provider buildId，调用/stream/callback 生命内继续使用该 owner；callback capability 固定 `ownerDeploymentBuildId`（route 只是 transport coordinate）；无 deployment activation generation 或全局 `RuntimeAssembly` owner。 | `runtime/boundary/src/value.rs` ＋ `runtime/loader` | missing | Phase 3A / 6A | TBD（Phase 0 审计） |
| R-072 | 576bbd31 | doc/architecture/any-interface-value.md | Method table/RemoteOperationSlot：slot signature 是 interface requirement 完成 type substitution 后的 canonical 调用形状，不含 `maySuspend`，也不允许任何 `inout` parameter mode；任何 `InterfaceMethod` call 携带 `inout` argument mode、或任何 method table/remote operation slot 声称接受 `inout`，都是 verifier 必须拒绝的 malformed artifact；object-safe 约束补充 receiver 与参数不得 `inout`。 | `runtime/linker`（verifier）＋ `artifact-model` | missing | Phase 3B / 6A | TBD（Phase 0 审计） |
| R-073 | 576bbd31 | doc/architecture/any-interface-value.md | Boundary contract：service operation 中除显式顶层 callback-capability projection 以外的参数/返回值默认 fail closed；boundary walker 区分 Package 入口签名（允许）、service operation 签名（只允许显式顶层 projection）、owner-internal recoverable boundary（按 carrier/self recoverability 判定）；任何 boundary 都不能携带 `inout` loan；顶层 `any I` 投影为 request-scope callback capability 时其 operation 只接收 detached value snapshot 不保留 caller-writable origin；same-process fast path 不改变该 ABI 事实。 | `runtime/boundary/src/plan.rs` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-074 | 576bbd31 | doc/architecture/any-interface-value.md | 顶层 `const` 不是 identity-bearing singleton：远程调用坐标只能指向 `api.yml` 显式公开并被 `service.yml.serviceCalls` 选择的 public instance root；普通顶层 `const` 是 deeply frozen value，其 logical snapshot 可按 value/COW 复用，不得为其隐式铸造 remote receiver identity；运行期临时对象直传可恢复字节（对端持有、回拨带回、构造侧在当次 exact buildId owner 中无状态重建等价副本），不铸句柄、不持有活实例、无注册表/GC。 | `compiler/source` ＋ `runtime/boundary` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-075 | 576bbd31 | doc/architecture/bytecode-vm.md | §1 不变量更新：Runtime 以 release pointer 解析出的 deployment `buildId` 为加载单位，loader 构建/验证/缓存 immutable `DeploymentExecutionImage`，旧 `RuntimeAssembly` record 删除；service/Actor/callback 同进程调用经 scheduler trampoline 切换独立 owner/fiber/heap，child 同步完成直接回 parent；`DeploymentOwnerIdentity` 以 exact buildId 为不可替代事实；同一 buildId image 内容唯一，并发 load 共享 per-buildId critical section，失败不发布半 image，LRU 逐出不影响 in-flight owner 强引用 pin。 | `runtime/loader/src/deployment.rs` ＋ `runtime/linked-program/src/shared_image.rs` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-076 | 576bbd31 | doc/architecture/bytecode-vm.md | §2.2 `ReleaseBundle` 只是离线清单：可列 deployment refs、聚合验证结果、提供可复现 bundle identity；不得产生 runtime-local executable address、不得成为 loaded-set key、不得出现在执行 identity、不得有 active/current、prepare/commit、generation、lease 或 switch 语义、不得作为缺 image 时的 fallback；test fixture 必须走同一 deployment loader/linker/verifier/VM。 | `artifact-model` ＋ `runtime/loader` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-077 | 576bbd31 | doc/architecture/bytecode-vm.md | §2.3 Service dependency resolution：`serviceDependencySlots` 只保存 `(serviceId, exact version, expected protocol identity, operation)` 等 contract facts，不固化 provider executable address；每次 service boundary call 由 boundary scheduler 解析 release pointer 得到 exact provider buildId、校验 protocol identity、加载或取得 provider image 并为该 invocation pin provider owner；不存在跨 service atomic generation snapshot；pointer 更新不影响已开始的 invocation/stream/callback。 | `runtime/boundary` ＋ `runtime/loader` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-078 | 576bbd31 | doc/architecture/bytecode-vm.md | §2.4 职责切分（7 项）：source analysis 是 effect/writable loan/capability provenance/escape/`maySuspend` 唯一 owner；emitter 负责控制流/stack effects/constant graph/relocation/frame layout/exception region/statement entry/source map/synthetic callback body 不制造 runtime address；structural validator 在 linker 前验证不可信 artifact；deployment linker 解析 relocation；semantic verifier 不信任 compiler/linker summary；VM core 窄职责；scheduler/adapters 拥有 child transfer/pending/wake/cancel/deadline/fiber queue/host I/O，adapter 不得保存可被 GC 移动的裸 pointer。 | 各 §6 owner | missing | Phase 1–5 | TBD（Phase 0 审计） |
| R-079 | 576bbd31 | doc/architecture/bytecode-vm.md | §3 ISA 更新：ISA 定义含 branch/call/tail call/return/throw/callback/effect invoke 语义与 verifier rules；wordcode 规则（operand 只保存 immediate/relative branch/slot/pool/relocation index；multi-operand 占多 word 不把 runtime address 强塞 u32）；`RelocatableBytecodeFunction` 含 effectSummaryRef；Package artifact 另保存 bounded frozen constant graphs、type/shape declarations、callback capture layouts、debug table（debug 名不扩大 runtime frame）。 | `artifact-model/src/bytecode.rs` | missing | Phase 1 | TBD（Phase 0 审计） |
| R-080 | 576bbd31 | doc/architecture/bytecode-vm.md | §3.3 Relocation 更新：relocation 至少区分 LocalExecutableRef/PackageCallableRef/ServiceOperationRef/ActorMethodRef/InterfaceRequirementRef/SyntheticCallbackRef/HostEffectRef/TypeRef/ShapeRef/FrozenConstantRef；`InterfaceRequirementRef` 只解析 interface identity/method slot/canonical signature，不能被 linker 伪装成 exact executable target，合法动态行为由 `call_interface` 等显式 opcode 表达；LinkedBytecodeImage 表扩展（exactLocalTargets/serviceOperations/actorMethods/interfaceTables/syntheticCallbacks/hostEffectAdapters/typeTags/shapes/frozenConstants/source-debug tables）；Package code 不因不同 deployment 被原地 patch。 | `artifact-model/src/bytecode.rs` ＋ `runtime/linker/src/linker.rs` | missing | Phase 1 / 3B | TBD（Phase 0 审计） |
| R-081 | 576bbd31 | doc/architecture/bytecode-vm.md | §3.4 指令族更新：新增 budget_checkpoint、call_service/call_actor/call_interface、interface_box_local/interface_box_remote/make_callback/invoke_callback、invoke_host；`copy_slot`/`dup`/container store/普通 by-value argument preparation 执行 semantic share transition（不能只复制 16 bytes 留下两个唯一 edit token）；`move_slot` 转移 value 使 source dead（verifier 证明不再读取）；`tail_call_local` 是显式 opcode，只由 emitter 在 source eligibility 已确定且 relocation 为 exact-local kind 时产生，由 post-link verifier 再证明 return plan 与 region eligibility；不存在 `call_suspend` 或无法验证的 `invoke_unknown`；restricted callback 有 make_callback+synthetic function，非开放 closure。 | `artifact-model/src/bytecode.rs` | missing | Phase 1 | TBD（Phase 0 审计） |
| R-082 | 576bbd31 | doc/architecture/bytecode-vm.md | §3.5 Decoded micro-ops：semantic verification 后可解码为固定 micro-op array 并 quicken（CallLocal/GetDenseField/CallInterface/InvokeHost）；superinstruction 只存在于 decoded/JIT 层，不得改变 semantic instruction charging、hard dispatch fuel、statement profiling、safepoint 或 source attribution；内部含无界循环的 micro-op 必须自行 poll。 | `runtime/vm` | missing | Phase 4 / 9 | TBD（Phase 0 审计） |
| R-083 | 576bbd31 | doc/architecture/bytecode-vm.md | §4 两阶段验证：pre-link structural validation 在 linker 读取任何 artifact-controlled index 前执行（magic/schema/ISA version、大小/嵌套深度/单对象上限、count/offset 算术无溢出、word 边界与 operand 数、index 界内与 declared kind 相容、jump/handler/resume target 指向 header、exception/source/statement/capture 表结构有序、frozen constant graph bounded 无 cycle、identity/content hash 一致）；structural validation 输出是 linker 唯一可消费的 typed validated view，失败不得进入尽量-link 路径；post-link semantic verification 至少证明列出的 14 项（exact target/arity/plan、CFG stack/type state、max depth、typed stack、三 carrier 同 canonical signature、exception region、resume descriptor、NoPending、tail_call_local、move/share/InOut alias、callback capture/escape、source 表覆盖、size 溢出、CFG cycle 经 budget_checkpoint）；只有 effect summary 的未知性可保守扩大，未知 target/type/shape/ABI/carrier tag/adapter identity 必须 fail closed。 | `runtime/linker`（structural + semantic verifier） | missing | Phase 3B | TBD（Phase 0 审计） |
| R-084 | 576bbd31 | doc/architecture/bytecode-vm.md | §5 maySuspend：`CallableEffectSummary` 继续计算（effectCategories/inOutPathEffects/resourceAndCapabilityProvenance/callbackProfile/invokesUnknownTarget/pendingEffectCategories/maySuspend=categories 非空）；普通 aggregate 参数/返回/throw payload 是 logical snapshot，旧 `writesCallerReachable`/`returnsCallerAlias`/`throwsCallerAlias` 不再是 aggregate effect fact；只有显式 `InOut` path 可写 caller place；resource/capability 用 identity-bearing provenance/lifetime descriptor；`maySuspend` 不改变 VM 物理 calling convention，但 concrete public Package callable 的 summary 仍是 Package Local ABI fact（变化触发重编译），也是 `NoPending`/`InOut`/sync callback/native context/verifier 可达性证明的输入；interface/conformance 与 ServiceContract 不承诺 implementation 的 `maySuspend`。 | `compiler/source/src/callable_effects.rs` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-085 | 576bbd31 | doc/architecture/bytecode-vm.md | §6 值布局：16B `ValueSlot`（payload u64 + metadata u64）；immediate 可含 null/bool/number/safe-immediate integer/date；reference 明确区分 RequestHeapRef/ActorStateRef/ConstRef/ResourceRef/CallbackClosureRef；非小型 payload 不以含多 String/Vec 的 Rust enum 占据每个 slot；`compactTypeTag`/object header `TypeTagId` 精确区分 nominal/named-union/catch leaf/interface payload identity；image 保存 TypeTagId→full stable identity/type plan，throw/catch/boundary/recoverable encode 经表展开不从 physical shape 猜测；managed reference 用 stable handle-table index 带 heap-space 与 stale-handle validation；stable handle 只解决地址移动不自动使对象存活（任何 allocation safepoint 都必须能从 root set 找到 handle）；request handle 不能进入另一 request/artifact/durable record/service payload。 | `runtime/model/src/value.rs` | missing | Phase 4 / 6B | TBD（Phase 0 审计） |
| R-086 | 576bbd31 | doc/architecture/bytecode-vm.md | §6.4 内存域：DeploymentExecutionImage/ConstantHeap（immutable 跨 request）、VmFiber（frames/slots/operand stack/regions/UnwindState）、RequestManagedHeap（owner-local request value graph）、ActorStateHeap（instance shared arena）、ResourceTable（显式 lifetime）、TransientRootStack（VM/adapter temporaries across allocation-capable code）；frame/slot/operand stack 不进 managed heap，用 request-owned contiguous segments 纳入统一 budget；stream/socket/file/timer/callback capability 进 ResourceTable，不能依赖 tracing GC 的不确定 finalizer。 | `runtime/model` ＋ `runtime/vm` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-087 | 576bbd31 | doc/architecture/bytecode-vm.md | §7 ConstantHeap initialization：顶层 `const` 是 deeply frozen、request-independent 值；source checker 只接受 deterministic/pure/`NoPending`/无 request-Actor-resource-callback-capability 的 initializer；compiler/build tooling 在受限 `ConstEvaluator` 执行（独立 temporary heap、deterministic intrinsic set、明确 budget），不得调用 runtime host adapter；求值失败/超预算/抛错/不可冻结 → package build 失败；artifact 保存 canonical `FrozenConstantGraph` 与符号 relocation 不保存可重试 initializer；pre-link validator 验证 graph 编码/大小/acyclicity/索引，semantic verifier 证明不引用 request/Actor/resource/closure/pending 或另一 image raw address；loader 在 image publication 前构造 immutable ConstantHeap，任一步失败则整个 buildId load 失败；constant load 返回 ConstRef 不产生 request allocation；首次 mutation 沿修改路径 thaw/COW，原 constant graph 永不改变；public-instance const 可含 frozen local behavior 并在 link 后取得 method table，但不能保存 request callback/live native handle/mutable singleton。 | `compiler/emission` ＋ `runtime/loader` | missing | Phase 2 / 3B | TBD（Phase 0 审计） |
| R-088 | 576bbd31 | doc/architecture/bytecode-vm.md | §8 aggregate value 表示：每 node 独立维护 owner/edit state（fresh→Unique→move 转移/copy→Shared→write path 沿路径克隆共享 node、share-transition 被复制的 child handle、mutate unique leaf）；复制 root 不立即遍历整图，首次写入先复制 shared root；single-owner heap 内 share state 不要求 atomic refcount（小 refcount/share epoch/sticky-shared bit，不能证明重新 unique 就保守 shared）；GC liveness 与 edit uniqueness 是两套事实不能互相替代；Array SmallFlat→UniqueFlat→SharedFlat、Map SmallLinear→UniqueHash→SharedHash；unique push/put 分别是 amortized O(1)/expected O(1)；shared 首写 path COW；literal/decode/DB result/map-filter/concat/merge/loop accumulator 用 transient builder，freeze 后才流动；`Array.concat` 先算总长一次/分段分配，左 operand 已死且 unique 可复用；persistent/HAMT 只在真实 benchmark 证明需要时引入；source API 不用 appending/setting 词形区分 purity：receiver `push/set/pop/put/delete` 只对 verified writable path（局部 var、有效 InOut loan、Actor method 允许的 self.field）mutation，pure transform 用 `Array.concat`/`Map.merge`。 | `runtime/model/src/value.rs` ＋ `compiler/lowering` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-089 | 576bbd31 | doc/architecture/bytecode-vm.md | §8.4 Dense records 与 canonical Map order：compiler-known record 用 ShapeId+dense ValueSlot fields，field opcode 带 verified offset 不查字符串 map；动态 JSON object/Map 用独立 hash representation；Map 用户语义要求 canonical key order（`keys()` 与 `for` iteration 操作开始时生成 snapshot，按 canonical string payload 的 UTF-8 bytes 升序）；mutation 继续用 hash table，只在 snapshot/encode 边界排序，不把 ordered tree 强加给所有 put。 | `runtime/model/src/value.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-090 | 576bbd31 | doc/architecture/bytecode-vm.md | §9 request-local GC/root：每普通 request owner 同一 GC-capable heap；无 short/long request class（duration/入口类型/await 次数不触发 GC）；完整 root set（runnable/blocked/waiting fiber slots、exception regions+UnwindState payload、parent/child invocation boundary state、PendingOperation roots、request/deployment context values、transaction cleanup state、ResourceTable payload roots、CallbackClosure captures、TransientRootStack entries）；ActorStateHeap 有自己的 roots/compaction contract 不混入普通 request collector；safepoint 在 allocation slow path/host effect enter-return/loop backedge checkpoint/scheduler handoff；cooperative single-owner。 | `runtime/model/src/request_heap.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-091 | 576bbd31 | doc/architecture/bytecode-vm.md | §9.3 Transient roots：allocation-capable instruction 优先在 allocation 成功前把 input operands 留在 VM slots/operand stack；若 VM/adapter 必须把 handle 暂存进 Rust local，必须在从 rooted slot 移除前 push 到 `TransientRootStack`，并在最后一个可能 allocation/safepoint 后 pop；cross-heap materialization 的 source slot/destination builder/partial error payload 适用同一规则；Pending owner 接管 handle 时 roots 从 transient stack 原子转移到 PendingOperation，不能出现两边都没 root 的窗口；debug build 在 safepoint 断言 adapter borrow 为空且 transient protocol 平衡；adapter 无分配 scope 内短暂 borrow 允许，allocation/GC/Pending/scheduler handoff 前必须结束；pin-all 不能代替该契约。 | `runtime/model/src/request_heap.rs` ＋ `runtime/vm` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-092 | 576bbd31 | doc/architecture/bytecode-vm.md | §9.5 unified memory budget：统计实际 owned/reserved capacity（heap pages/handle table、VM stack pages、all owner-local child heaps、pending buffers/roots、collection/string capacities、transaction journals、resources、callback captures、boundary materialization buffers）；Actor arena 有 per-instance 独立上限，本次 invocation 增量工作计入 request execution budget；soft limit 先收集，hard limit 结构化 memory resource error；request terminal 释放所有 fiber/owner-local heap/pending owner/resource。 | `runtime/request/src/runner.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-093 | 576bbd31 | doc/architecture/bytecode-vm.md | §10 frames/local call/tail call/InOut：VmFiber 状态扩展（Runnable/BlockedOnChild/WaitingHost/Unwinding/Terminal + owner + unwind + actorContinuation）；Frame 无 instantiatedTypeContext（链接期已 concrete）；进入 frame 按 verified max depth 一次扩展；non-tail recursion 不增长 native stack 但受 frame/memory/fuel 限制；`call_local` 以 move/share 准备 slots 同一 loop 继续；`tail_call_local` 在 caller frame live 时求值参数一次，验证共同 return plan/self/generic/region eligibility 后替换；每 hop 保留 charge 与 hard fuel；`InOut` 是 Package Local only 的显式 caller-writable write-through loan（exact local/package-direct callable、writable var path、compiler/verifier 证明 exclusive、callee verified NoPending、unique 原地写/shared 时 path COW、ordinary throw 不回滚已执行写入、禁止位置清单：service/gateway/Actor external/interface/callback/durable-recoverable/host effect、不允许被 concurrent sibling 共享或逃逸到 callback/resource）。 | `runtime/vm` ＋ `compiler/lowering` | missing | Phase 4 | TBD（Phase 0 审计） |
| R-094 | 576bbd31 | doc/architecture/bytecode-vm.md | §11 scheduler trampoline：单个 native dispatch driver 迭代处理 `VmControl`（Continue/Complete(Result)/EnterChild/Park）；host adapter 启动结果为 `EffectStart`（Ready(Result)/Pending），Ready 明确包含同步失败（error 在当前 source site 注入，不需先制造 future/Pending），只有可信 host adapter 可返回 Pending；service/Actor/callback dispatch adapter 可返回 `BoundaryStart`（Ready/EnterChild/Pending）；`EnterChild` 使 parent 进入 BlockedOnChild，scheduler 把 child fiber/owner/heap 压入 invocation stack 同一 loop 立即执行，不把 parent 放 waiting table、不释放 Actor lease、不计 suspension；child 同步 Complete 按 boundary plan materialize 并继续 parent（native stack 扁平）；child 真实 host Pending 时保存 parent/child chain 并 park leaf，调用链上 Actor lease 按各自规则释放。 | `runtime/vm` ＋ `runtime/request/src/runner.rs` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-095 | 576bbd31 | doc/architecture/bytecode-vm.md | §11.3 Park/resume：Park 前 site 已有 verified stack/result/resume descriptor；fiber 至少保存 pendingId/resumePc/expectedStackHeight/resultPlan/sourceSite/invocationChain/unwindPhase（cleanup pending 时）；arguments 在 adapter contract 下完成 move/materialize/root transfer；fiber 进入 waiting table；scheduler 拥有 PendingOperation、deadline/internal-stop registration 与 wake；frame chain 不复制不序列化；wake result 为 Value/Throw/internal terminal；失败在原 effect site 注入走同一 unwind state machine；budget/profiling/type context/diagnostic prefix/transaction region 不重置。 | `runtime/vm` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-096 | 576bbd31 | doc/architecture/bytecode-vm.md | §12 `call_interface` 三 carrier：operand 含 verified interface table/method slot/arity/canonical signature；Local（concreteType+methodTable+payload → push exact local frame 同 owner/heap）、RemoteService（dependencySlot+publicInstance+operationTable → boundary resolution 后 EnterChild 或 transport Pending）、CallbackCapability（capability owner+operationTable+request lifetime → EnterChild 回 owner 或 transport Pending）；verifier 证明 method slot 与三分支参数/返回 shape 等于 interface requirement，image construction 验证每个 local method table exact target 与 remote operation table；unknown carrier/tag/slot fail closed；local 分支静态 maySuspend 保守 true 也不 yield。 | `runtime/vm` ＋ `runtime/linker` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-097 | 576bbd31 | doc/architecture/bytecode-vm.md | §12.2 restricted callback bodies：目标静态语义已允许 IIFE、白名单 callback argument、具名 callback reference，bytecode ISA 不能把 closure 全推未来；emitter 把每个 callback body outline 为 `SyntheticCallbackFunction`（exact signature/effect summary/capture layout/source map/普通 bytecode body）；immutable/value capture 经 copy/move 进 closure env，writable capture 用显式 fiber-owned cell descriptor（仅 non-escaping callback profile 内合法）；`make_callback` 创建 `CallbackClosureRef`，capture slots 是 GC roots，adapter 跨 actual Pending 持有 callback 必须把 closure/root 所有权登记到 PendingOperation；IIFE 直接调用 synthetic function 不物化 general closure；callback 不能进入普通 record/container、返回、durable/recoverable payload 或越过静态 lifetime（verifier 交叉检查 escape sites）；不提供任意 first-class function/unbounded escaping closure/隐式 callback conversion；service API 顶层 `any I` callback capability 不是 FnExpr closure，由 boundary projection 产生（显式 deployment owner、interface operation table、request/stream lifetime），不能进入 recoverable/durable payload。 | `compiler/emission` ＋ `runtime/vm` | missing | Phase 2 / 6A | TBD（Phase 0 审计） |
| R-098 | 576bbd31 | doc/architecture/bytecode-vm.md | §13 exceptions/regions/UnwindState：exception table 每函数至少含列出的字段；catchMatcher 是 linked catch-leaf/type-tag matcher；active region 可拥有 timeout/DB transaction-lease/structured join/stream supervision/resource lifetime cleanup；Actor invocation 没有 commit overlay 只登记 segment lease/fence cleanup；commit/abort/lease release/stream-resource close 都可能 Pending，因此 fiber 显式保存 `UnwindState`（reason/primaryTerminal/regionCursor/cleanupPhase: Enter/Starting/Waiting/Completed/Done）；规则：primaryTerminal 一旦选择不因 resume/re-poll 丢失；每个 cleanup action 有稳定 action id 与单调 phase；normal transaction exit 先 commit，commit 失败以 commit error 为 primary 再尽力 abort，abort failure 不覆盖原 error；ordinary throw 在 commit 选择前等待 abort 保留原 throw；timeout/internal stop 的可见 terminal 立即固定，best-effort cleanup 可在 bounded owner 继续但不延迟已固定 response 也不能把 late result 写回结束 heap；cleanup 自身调用 child/host 时继续同一 trampoline/actual-Pending 协议。 | `runtime/vm`（unwind） | missing | Phase 4 / 6A | TBD（Phase 0 审计） |
| R-099 | 576bbd31 | doc/architecture/bytecode-vm.md | §13.3 transaction state：nested transaction 不支持（source checker 与 verifier 都拒绝）；`db transaction` 的 atomicity 只属 DB driver transaction（commit 发布 DB write，abort 只回滚未提交 DB write）；VM 不为 local slot/request heap/`InOut` place 建立 root checkpoint/journal；throw/commit failure/abort 前已执行的普通内存写入继续可见；Actor transaction 遵守同一 DB-only 规则，body 禁止直接或经 callee 写 actor field，该禁令是 compiler/verifier effect rule 不是内存 rollback 机制，不得为 Actor 重新引入 snapshot overlay。 | `runtime` ＋ `compiler/source/src/callable_effects.rs` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-100 | 576bbd31 | doc/architecture/bytecode-vm.md | §14 Actor shared heap：每 live Actor instance 拥有 ActorStateHeap shared arena 与 stable field root slots；method entry acquire ActorSegmentLease 并校验 identity/implementation build-identity/fence/arena epoch；同步段直接读写，已执行 mutation 立即可见；ordinary return 只归还 lease；actual Pending 才 drop guard/lease，fiber 保存 `ActorContinuation { identity, implementationBuildId, fence, arenaEpoch, leaseState }`；resume 前重新 acquire 并重新校验，升级/逐出/fence/epoch 不匹配按 terminal contract 失败；request-scoped stream/resource/callback capability 在 actor field 写路径立即 fail closed；compaction 只在 quiescence 执行并 bump arena epoch；跨 owner service/callback child 同步运行期间 caller Actor lease 保持不释放，child 实际 Pending 才释放。 | `runtime/eval/src/actor_*` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-101 | 576bbd31 | doc/architecture/bytecode-vm.md | §15 boundary/recoverable：service/Actor/callback boundary 总是按 typed value plan materialize（caller owner+heap → canonical boundary value/freeze → provider owner+fresh heap/Actor arena → execute child → canonical result/error → caller heap）；provider 不接收 caller frame、mutable root、`InOut` loan、request handle 或 Actor execution frame；同进程 EnterChild 只省 transport/native future 不省 boundary 语义；source/destination temporary values 遵守 transient root protocol；recoverable codec 只观察 logical typed value graph 与 stable code/type/carrier identity，不保存 ValueSlot bytes、raw handle、unique/share/edit token、COW backing identity、GC generation、fiber/frame/pc/resume/pending/unwind state、InOut loan、transient root、live callback/resource capability；共享 physical backing 编码成等价 acyclic value tree；cycle 按 recoverable contract fail closed；ConstRef 编码其 logical value/code identity，decode 到当前 owner fresh value，不能把 image-local const index 当 durable identity。 | `runtime/boundary/src/recoverable.rs` | missing | Phase 6A / 6B | TBD（Phase 0 审计） |
| R-102 | 576bbd31 | doc/architecture/bytecode-vm.md | §16 hard fuel/budget/attribution：artifact semantic charge metadata 不能单独承担安全性；VM dispatch 每执行一条 semantic instruction 更新受信 hard fuel counter，耗尽必须检查 instruction limit/deadline/internal stop 后再继续；artifact 不能关闭/跳过/重置该 counter；post-link verifier 另证明每个 CFG cycle 经过 `budget_checkpoint`（保持语言可观察 budget 单位与及时 stop）；两层同时存在；decoded fusion/JIT 必须按所含 semantic op 数量扣 fuel 并在内部 unbounded loop poll；emitter 为 statement/expression/function entry/local call/tail hop/loop/generated chunk 生成稳定 charge metadata；statement entry 恰好记录一次 profiling unit；source map 覆盖 call/throw/effect/DB/timeout/generated instruction；挂起/恢复不重复不遗漏 statement；frame 保存 exact call site；tail replacement 遵守 bounded diagnostic contract；cross-service frame 经 canonical error channel 投影。 | `runtime/vm` | missing | Phase 4 / 9 | TBD（Phase 0 审计） |
| R-103 | 576bbd31 | doc/architecture/bytecode-vm.md | §17 performance contract（更新版）：sync local call/return 零 host heap allocation 零 Rust future；同步完成的跨 owner bytecode child 不 park 不递归 native stack；slot 操作固定宽度；semantic copy 的 share transition 为 O(1) root 操作；constant load 零 request allocation；dense field 是 verified offset；unique Array push amortized O(1)、Map put expected O(1)；nested snapshot 只沿首次 write path COW 不 deep traverse；builder 不逐元素复制；Map mutation 不为 canonical iteration 维持 tree；低分配 request 零 GC；Ready success/error 不分配 continuation；Pending 只创建一个 pending owner 并移动现有 fiber/invocation chain；tail recursion O(1)；allocation-heavy request 可回收中间值；Actor 同步段零 clone/encode/commit overlay，actual Pending 只 lease release/reacquire；quickening 不扩大 ISA；release benchmark 至少覆盖列出的 13 类 + 真实 Agine chat smoke。 | `runtime/vm` ＋ benchmark tooling | missing | Phase 4 / 9 | TBD（Phase 0 审计） |
| R-104 | 576bbd31 | doc/architecture/bytecode-vm.md | §18 completion contract（更新版）：runtime-lazy-load-deployment.md、package-service-contract-deployment.md 与 runtime 实现都以 buildId DeploymentExecutionImage 为执行单位；reference 收敛到 value semantics/writable path/Package-only InOut；Package/Service 契约拒绝 boundary InOut 而 Package Local ABI 保留；schema 单一声明；pre-link validator 不可绕过、post-link verifier 对未知 target/type fail closed；const evaluator+ConstantHeap 落地；EnterChild/Ready/Pending 矩阵共享 trampoline；restricted callback 与三分支 call_interface 有真实 source-to-runtime test；owner 分离；focused test 清单；hard fuel 与 CFG-cycle checkpoint 共同阻止无 charge loop；真实路径/GC pressure/Agine chat smoke 通过；tree evaluator/old reader/assembly admission-generation/call_suspend/test-only evaluator/compatibility fallback 全部删除；新语义不能以旧 evaluator 作 differential oracle（reference-derived golden test）。 | 全局（所有 §6 区域） | missing | Phase 8 | TBD（Phase 0 审计） |
| R-105 | 576bbd31 | doc/architecture/compiler-entity-and-identity.md | Entity identity 与 runtime address 边界更新：`RuntimeAddress` 定义为某个 linked deployment image 中的执行地址或 slot（不是某次 activation/linking）；runtime address 不得写回 artifact DTO，artifact/projection 不得依赖某次 image load 的 address；`TypeAddr` equality 不作为 artifact/ABI equality across deployment images；`config` resolver root 指向当前 Package 声明、由 deployment config plan 提供的 config requirement API。 | `compiler/driver` ＋ `artifact-identity` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-106 | 576bbd31 | doc/architecture/compiler-package-pipeline.md | 三 artifact 模型：Package 是唯一源码编译单元；ServiceContract 与 ServiceDeployment 是独立 projection；可选 release bundle 不属于 compiler source pipeline；无 Package/Service 共同 `Publication` 输入/产物/流水线；crate 依赖方向固定（compiler source/lowering/projection → skiff-artifact-model → skiff-artifact-identity；runtime loader/linker → artifact-model+identity；router → strict routing DTO+transport）；`skiff-artifact-model` 不得依赖 compiler/runtime/Router；`skiff-artifact-identity` 只消费 artifact-model typed DTO；Router 不拥有 canonical hash、不依赖 compiler AST/lowering 或 runtime evaluator/VM，无法共享实现时用同一 schema 与 language-neutral golden fixture 验证 parity 不能复制独立 identity 算法；`PublicationAbiUnit`/`PackageUnit`/`ServiceUnit`/`CompiledPublication`/`LoweredPublication` 或共同 projection bundle 均不是合法 owner。 | `artifact-model` ＋ `artifact-identity` ＋ `compiler/driver` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-107 | 576bbd31 | doc/architecture/compiler-package-pipeline.md | ServiceDeployment projection：消费 typed code artifacts 与受信 profile 选择的三层 config；闭合 exact package dependency bindings；service dependency 只生成 coordinate/expected protocol/used operation slots，不绑定 provider build/address；按 exact Package closure 验证 config requirements 并生成 deployment-owned protected payload（canonical artifact store writer 的 store-domain deterministic `BakedConfigPayloadRef`）；生成 immutable ServiceDeployment、payload ref 与 revision/build identity；业务配置值不进入 PackageArtifact/ServiceContract，Secret 明文不进入公开 identity preimage；config 没有独立 projection 或发布生命周期，ref 算法只由 package-service-contract-deployment.md 定义。 | `compiler/driver/pipeline/mod.rs` ＋ `artifact-identity` | missing | Phase 2 / 3A | TBD（Phase 0 审计） |
| R-108 | 576bbd31 | doc/architecture/db-capability-architecture.md | DB capability 边界更新：service DB 连接能力由 Router connection bootstrap 注入；`db transaction` 一次 execution 只允许一个 active DB transaction，nested 不支持（compiler 拒词法可见嵌套，Runtime 拒 helper 动态重入，不能把内层静默折叠进外层）；Actor method（含 create）中 transaction 是 DB-only（不为 Actor arena 建 snapshot overlay，body 禁直接或经 callee 写 Actor field，直接赋值与以 field 为 receiver 的原地修改都算，effect summary 闭合同包 helper 与 package-direct call，unknown/dynamic 保守拒绝）；普通 local 用 `let`/`var` 不存在 local `const`。 | `compiler/source/src/callable_effects.rs` ＋ `runtime/host`（service_db） | missing | Phase 6A | TBD（Phase 0 审计） |
| R-109 | 576bbd31 | doc/architecture/db-capability-architecture.md | Exact Service DB Index Plan 是 image-local derived metadata：runtime loader 为每个 exact DeploymentExecutionImage 从该 deployment 自己的 exact package closure 与 linked DB metadata 派生完整 plan；不从运行中 request、current release pointer、其它 loaded image 或 Mongo 反向推断；受管 index stable identity 是 `(packageId, logical collection identity, logical index identity)`（version/build/service version/alias/edge/replica 不参与）；同 identity 定义不同、metadata owner 冲突、physical encoding collision、同 Package ID 解析到不同 build 都在任何 Mongo mutation 前拒绝 image；每个 replica 在 image 记为 loaded 前独立 reconcile（missing 幂等创建+复读验证、定义不一致 fail closed、未声明 index 保留且忽略、非 Skiff 与 `_id_` 保留）；无跨 replica/build 协调协议；storage DDL 串行化不改变契约；ConstraintError 统一脱敏分类；partial index 不支持。 | `runtime/loader` ＋ `runtime/host`（service_db） | missing | Phase 3A | TBD（Phase 0 审计） |
| R-110 | 576bbd31 | doc/architecture/db-capability-architecture.md | Router 不解析 index metadata、不协调 prepare/commit/abort、无 global candidate generation/activation prepare；Runtime 按 exact deployment build 构建 image 时在业务 storage mutation 前验证 service DB identity 与完整 index plan；同 replica 内同 build 并发 load 按 per-build 临界区收敛；已有某些 index 不能跳过其余 plan 验证；失败不得把半可执行 image 记为 loaded/注册；只有 deployment package closure 含 DB metadata 才按需创建 service DB handle。 | `router` ＋ `runtime/loader` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-111 | 576bbd31 | doc/architecture/durable-task-dispatch.md | Runtime submission side 只能使用当前受认证 `DeploymentExecutionContext` 注入的 task capability；task control plane 把提交方 authority 精确投影成 task owner 与 trusted execution witness，TaskStore 不接受 Runtime 自报的 service/build/display name。 | `router/src/task/admission.rs` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-112 | 576bbd31 | doc/architecture/durable-task-dispatch.md | 执行镜像与 target pinning：task 提交时冻结完整 execution image identity（核心是 exact deployment `buildId`）而非可读 target 名字；`TaskExecutionImageRef` 是提交时 exact buildId 及其 immutable artifact facts 的受认证引用，不包含提交 request 的物理 replica 或任何 ambient release state；到期 attempt 把 exact buildId 交给普通 Runtime selection/lazy-load 路径（已加载或具备同一 artifact store 懒加载能力的 Runtime），load 失败按平台 attempt 失败/重试收敛，不解析 release pointer 不 fallback latest；`buildId` 是 Runtime 唯一加载键，TaskStore 不保存 `RuntimeAssembly`/current assembly/activation generation；非 terminal task 是 image/artifact retention root。 | `router/src/task/admission.rs` ＋ `runtime/loader` | missing | Phase 3A / 6A | TBD（Phase 0 审计） |
| R-113 | 576bbd31 | doc/architecture/durable-task-dispatch.md | Actor-method target：`TaskExecutionImageRef` 以 exact deployment buildId 唯一钉住延迟调用的代码与配置，service version 保留可观测版本事实，`ActorImplementationIdentity` 用于 target 与 Actor admission 校验；task 保存 ActorIdentity + exact buildId + implementation/method identity + `ActorActivationSnapshot`；到期执行 get-or-activate 前 Runtime 先按 task 的 exact buildId 取得/懒加载 image，load/verify 失败不能用其它 build 替代；分支 1 live incarnation 同 implementation identity 时按普通 admission 排队并从 task exact-build image 执行（identity 相同不授权改用 live owner 碰巧加载的其它 build）；分支 2 registry entry 存在时按 entry 创建输入从 task exact-build image 激活；分支 3 entry 丢失用 snapshot 恢复；分支 4 升级 forward target 触发/等待升级；分支 5 已由新 implementation 接管时拒绝（`ActorVersionRejectedError` → platform-failed）；`ActorActivationSnapshot` 与激活都是 Actor 实例生命周期术语不是 deployment activation。 | `router/src/task/actor_attempt.rs` ＋ `runtime/loader` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-114 | 576bbd31 | doc/architecture/durable-task-dispatch.md | Claim/lease/at-least-once：scheduler 只 claim `due_at <= now`、state ready、task exact buildId 的 retained artifacts 仍可加载且 policy 有容量的 task；claim 原子写 state/lease/attempt generation；Runtime admission 从 exact-build image 发起普通 request（fresh request-local context；function target 用 fresh RequestHeap；actor-method target 只使用边界 decode scratch，参数以 value snapshot 导入 instance-owned shared arena，不创建逐方法 heap 或字段副本）；actor-method target 同步段由 `ActorSegmentLease` 串行、actual Pending 释放后才与其它协程交错；task lease 不替代 actor owner lease；at-least-once 只在 exact-build image 可加载且基础设施恢复时成立。 | `router/src/task/` ＋ `runtime` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-115 | 576bbd31 | doc/architecture/gateway-runtime-adapter-boundary.md（D） | Retirement：gateway/Runtime adapter 边界契约文档删除；其概念（HttpGateway/RuntimeDispatcher/RuntimeRegistry/WebSocketRequestBroker/adapterArgs/connection policy/ranked admission/JSON-RPC 编解码分层等）分别由 router-rust.md、runtime.md、service-yml.md、std-surface.md、http.yml/websocket.yml 接管；代码审计要求：router 侧 gateway 代码保留并按新文档验证，旧概念（assembly generation pin、ActivationContext 注入、旧 frame 代际）对照代码删除；不把被删文本当现行契约。 | `router/src/http/` ＋ `router/src/session/` | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-116 | 576bbd31 | doc/architecture/managed-dev-watch.md | Watch 范围收缩：只定义本地 registry、fingerprint、publish、withdraw 与重试契约；deployment release/pointer/Runtime lazy-load 语义归 runtime-lazy-load-deployment.md；不定义 artifact DTO、跨 service 事务或 Runtime load 协议；registry v2 结构（canonical kind+root、serviceId、排序唯一性）、两阶段读取（结构/live validation）、原子写（临时文件+flush+rename+父目录 sync）；add 先 live validation；remove 按 root 与 service ID 唯一命中否则 fail closed。 | `scripts`（tooling） | missing | Phase 3A | TBD（Phase 0 审计） |
| R-117 | 576bbd31 | doc/architecture/managed-dev-watch.md | 状态与收敛：watch 持有 lastKnownGoodRegistry/pendingFingerprint/lastSuccessfulFingerprint/ownedReleasePointers（key→lastPublishedBuildId）；fingerprint 覆盖 profile、canonical root 集合、每 root 完整 build 输入、dependency resolution 与 toolchain identity（Router health/loaded set/pointer mtime/上次错误不进入）；配置输入只以 `BakedConfigPayloadRef` 进入 fingerprint，不保存 Secret 明文或 unkeyed digest；ownedReleasePointers 只记录自己发布过的 pointer 不能扫描整个 profile 声称归属；收敛流程：编译/发布 immutable records → 每 service root 原子更新 `(profile, serviceId, exactVersion) -> buildId` + ledger → withdraw 只在 pointer target 仍等于 ledger 旧 buildId 时原子 unset，外部更新则报 ownership conflict 不覆盖；多 service 收敛由独立单键 pointer update 组成，读者可观察新旧 build 混合，watch 不得在这些 pointer 之上加共同 commit 点；失败重试退避 1s→30s，新 fingerprint 替换旧 pending；watch 退出只停止本地循环不改 pointer。 | `scripts`（tooling） | missing | Phase 3A | TBD（Phase 0 审计） |
| R-118 | 576bbd31 | doc/architecture/observability-requirements.md | 运行归属：执行事件必须同时包含 exact deployment `buildId`（已 pin 的 ServiceDeployment build id，不是 Package build id 或 release pointer 当前值）、`runtimeId` 与 `runtimeSessionId`（session epoch）；通用运行归属不得使用 deployment activation identity、runtime activation identity 或 assembly generation；Actor activation id 与 client socket generation 只能留在语义明确的 Actor/socket 生命周期事件中。 | `runtime`（telemetry producer） | missing | Phase 8 / 9 | TBD（Phase 0 审计） |
| R-119 | 576bbd31 | doc/architecture/observability-requirements.md | Runtime 指标集：VM fiber create/runnable/park/resume/complete 计数、ready queue 深度与 park 时长；跨 service/Actor/callback owner transfer 次数/耗时/terminal-rejection 分类；managed heap allocation bytes/objects、current/peak、limit rejection、GC 次数/pause/reclaimed；deployment image load/link/verify 分阶段耗时、cache hit/miss/wait、load rejection 按 missing/decode/structural validation/link/semantic verification/resource limit/timeout 分类；均可按 buildId/runtimeId/runtimeSessionId 聚合；trace 子 span 携带 provider 的 buildId/runtimeId/runtimeSessionId；日志自动附加这些上下文。 | `runtime`（telemetry） | missing | Phase 9 | TBD（Phase 0 审计） |
| R-120 | 576bbd31 | doc/architecture/open-issues.md | 开放问题更新：`dispatch` 已定义唯一 detached-call surface（TaskRef/status/before-start cancel），内部 TaskStore/lease/timer 契约归 durable-task-dispatch.md；用户可见的结果型 async task、cron、startup/managed worker 仍未规范化（后续版本范围）。 | `TBD`（未来能力，非本 scope 实现） | missing | 后续版本（非本 scope） | TBD（Phase 0 审计） |
| R-121 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | 三 canonical artifact 模型：PackageArtifact/ServiceContract/ServiceDeployment（无 RuntimeAssembly）；可选 `ReleaseBundle` 只聚合 exact ServiceDeploymentRef 与 verification receipt ref/receipt，BundleIdentity 从这些事实确定性计算，不是 runtime load/routing/request 单位；业务配置在发布时冻结为 deployment-owned protected payload，deployment 保存不可替换 opaque ref，ref 参与 buildId，配置没有独立 selector 或发布生命周期；三类 artifact 无共同 aggregate；不变量 9-12 更新为 replica 按 buildId 独立懒加载 image、identity 分离（code/API/deployment/buildId/bundle）、每个 request-scoped `DeploymentExecutionContext` 随 fiber/stream/callback 显式传播（service child 切到 exact provider build owner，callback 切回 capability 记录的 exact owner，普通 continuation 不得从 ambient context 补 owner 事实）、actor/dispatch 携带其契约要求的 exact deployment build/actor implementation owner。 | `artifact-model` ＋ `runtime/loader` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-122 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | Ingress 两阶段选择更新：Router 先用 release pointer 解析唯一精确 `ServiceDeploymentRef`/buildId，再只在该 deployment 内按 `IngressSelector` 选 gateway entry；HTTP Host 不是 IngressSelector 字段；缺失/非法/歧义 selector、pointer 记录不兼容、跨 build 替换都 fail closed；Router 发出的 request frame 携带精确 deployment/buildId；WebSocket 连接 pin exact build 不能从 Host 或 ambient connection state 重推。 | `router/src/http/` ＋ `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-123 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | `PackageLocalAbi` 可显式包含 `InOut` parameter mode（只表达 `NoPending` exact package/local call 期间的 exclusive caller-writable loan）；普通参数仍是 value semantics；`InOut` 不得投影进 ServiceContract、gateway/interface/callback/Actor external ABI、host effect ABI 或 recoverable payload；config 三文件只构造发布时冻结给 ServiceDeployment 的 typed protected config payload，不改变 package/service dependency graph 或 Package/ServiceContract identity，配置变化产生新 deployment buildId；含 `InOut` 参数的 public callable 的 service projection 必须为 `Unavailable(InOutNotAllowedAtServiceBoundary)`。 | `compiler/lowering` ＋ `artifact-model` | missing | Phase 2 / 3A | TBD（Phase 0 审计） |
| R-124 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | ServiceDeployment 形状：serviceId/packageVersionLabel/expectedProtocolIdentity/deploymentRevision/deploymentBuildId/implementation PackageArtifact ref/operationBindings/packageDependencyBindings/serviceDependencySlots（coordinate+expectedProtocolIdentity+usedOperations）/bakedConfigPayloadRef/gatewayEntries；deployment validation 保证 operation/gateway binding 完整、package/service 依赖唯一 binding；deployment 可换 package build 保持 service id/version label（API identity 不变），变化由 revision/buildId 表达；业务配置变化同样生成新 immutable deployment/buildId 并原子更新 release pointer，配置不能单独发布。 | `artifact-model` ＋ `compiler/driver` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-125 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | 两类调用：package dependency 调用用 PackageLocalAbi 与 implementation links（同一 DeploymentExecutionImage/owner 内直接调用复用当前 managed heap；普通参数 aggregate value semantics 按 liveness move/share/COW；只有显式 InOut 可 write-through；不经过 service dispatcher 不切换 deployment owner；不做 boundary materialization 不要求 recoverable）；PackageDirect stream producer 复用当前 request 已有 StreamRuntime registry，不把 handle 当 boundary value 重新 materialize，不把 package-local registry 共享规则扩张到 service boundary；File IR 对 package direct call 使用唯一 canonical target 不携带 legacy publication operation ABI。 | `compiler/lowering` ＋ `runtime/boundary/src/stream.rs` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-126 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | Service boundary call：service dependency 调用只解析到 ServiceContract operation，consumer lowering 不链接 provider executable 不生成伪 PackageArtifact，保存 `ServiceCallRef`（serviceRequirementSlot/contractOperationId/expectedProtocolIdentity）；执行 call site 时 boundary scheduler 按当前 profile 解析 slot 的 `serviceId + exact version` release pointer 得到 provider buildId，验证 protocol identity 满足 caller 冻结的 expectation，取得或 lazy-load immutable provider image；pointer 缺失/record 不匹配/provider load 失败都在进入 provider 前 fail closed；物理 binding 两种但语言 boundary 相同：InProcessBoundary → VM scheduler EnterChild(provider fiber+fresh heap)，RemoteBoundary → runtime transport Pending/response；进程内路径不能因地址可见传递 caller heap handle/mutable root/InOut loan/local method table，只省 transport 和 native future；provider 同步完成时 child trampoline 直接返回不制造虚假 Pending；只有 child/transport 实际等待时 caller fiber 才 park、caller Actor 才释放 segment lease。 | `runtime/boundary/src/plan.rs` ＋ `runtime/vm` | missing | Phase 5 / 6A | TBD（Phase 0 审计） |
| R-127 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | 原子重绑定：deployment-scoped owner 全部替换为 exact provider build 的事实（ConfigView/service DB/file capability/actor registry/dispatch/WebSocket service-entry lookup/telemetry attribution/service dependency slots）；request-scoped owner 保持同一 request 事实（deadline/internal stop/clock/request lifecycle/trace/error channel/transport request identity/stream lifecycle/test effects/heap limits）；provider 必须获得 fresh request-local heap，参数按 contract value plan materialize；caller call frame/slot/mutable root/`InOut` loan/Actor execution frame 不进入 provider；Package 静态资源按 provider image 解析；`ActorRef` 显式 owner 不被重写；service slot 逻辑 key 是 `(callerDeploymentBuildId, callerPackageBuildId, serviceRequirementSlot)` 不是裸 slot index；普通挂起/恢复与 stream producer/consumer 保留创建时的 exact deployment owner，不依赖 thread-local 当前 service；只有 service provider entry 与 callback capability dispatch 是 owner transfer 入口。 | `runtime/host/src/host/router_session.rs` ＋ `runtime/boundary` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-128 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | 一次 service invocation 开始时 pin provider build；返回 stream 时 producer/consumer bridge/callback/每 item materialization 继续使用该 build 直到 stream end/error/drop；release pointer 更新不迁移既有 stream；新 invocation 重新解析 pointer；Skiff 不提供跨 service 的 ambient atomic release snapshot；三层契约（ServiceContract 位置无关语言语义 / Binding ABI InProcess|Remote / PackageLocalAbi provider 内部本地代码 ABI），本地与远程 binding 不需要相同机器 ABI 但实现同一 ServiceContract。 | `runtime/boundary/src/stream.rs` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-129 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | External ingress：HTTP/WebSocket gateway entry 不是 service boundary call；Router 先按可信 header 经 release pointer 选择精确 buildId，再按 IngressSelector→GatewayEntryKey→GatewayEntryIdentity 进入 deployment owner，由 deployment gateway entry binding 冻结的精确 `PackageCallableId` 执行 handler；不经过 service dependency slot 不伪造 ContractOperationId；ingress 复用普通语言函数/Package 本地链接/DeploymentExecutionContext/错误通道/internal stop，不把 ServiceContract 作为对外声明；WebSocket 发送从当前 DeploymentExecutionContext 解析唯一 entry；Peer request 按 socket pin 的 deployment build 与 transport connection incarnation 解析 method。 | `router/src/http/` ＋ `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-130 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | WebSocket request/response broker：outbound pending 与 inbound active 的 pending identity 至少含 direction、物理 socket/generation、connection id、编码配置 id、peer-visible request id；response 不能跨 connection/重连 generation/service-entry 归属命中；同 connection 两方向可乱序完成，每 pending/inbound 最多完成一次；disconnect 原子移除 mapping 并本地内部停止；deadline/internal stop 先移除 pending 不向 peer 发 cancellation notification；outbound 保留有界短期 response tombstone，inbound 保留 settled-id tombstone；unknown response id 属协议错误；outbound/inbound/single-payload/tombstone 分别受平台 limit 约束（outbound 满 fail closed，inbound 满 `-32000 Server busy`）；transport 不自动 retry；平台 parse/invalid/method/params/internal error 分别 `-32700/-32600/-32601/-32602/-32603`，容量/timeout `-32000/-32001`；notification 一律不 dispatch 不 response；batch 不执行并返回单个 `-32600`；duplicate active/settled id 以 `1002` 关闭。 | `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-131 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | WebSocket connection model：policy key 是 `(serviceId, websocketEntryId, businessIdentity)`；version/build 不进入 policy key；`admissionRank` 是 service 在持久化数据库事务中递增保存的 fencing token（正 safe integer，Router 不分配不推导，重启后从持久化计数器继续）；ranked admission 只允许精确组合 businessIdentity+maxConnections:1+close-oldest+admissionRank；ranked high-water（Router-local，存在 active connection 时必须保留，硬容量上限）+ global new-key quiet-window；ranked loser/capacity refusal 走先 101 再 close 路径；第一版每 service 最多一个 WebSocket entry；无 connect handler 时 gateway 直接合成 accept。 | `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-132 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | Payload/schema 边界：Runtime 拥有业务 payload encode/decode（linked program 类型 → `RuntimeTypePlan` → boundary contract）；Router 转发 opaque bytes/opaque JSON 不解析业务类型表示，不引入 closed-vocabulary type descriptor；JsonSchema 只用于外部协议校验/文档/diagnostics 不是 runtime codec source of truth；compiler 只为 adapter plan 中连接 external ingress 的值派生 entry-local schema；entry-local component key 由 canonical external shape 或显式 external documentation metadata 产生，不能借用 Package public identity；HTTP 业务 schema 不得声明 requestId/correlationId 伪字段（真正的业务 id 按语义命名如 idempotencyKey/jobId/runId）；平台 WebSocket transport id 不进入业务字段。 | `runtime/boundary` ＋ `router` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-133 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | §7 recoverable/boundary 更新：ordinary data 按 contract 生成 detached value graph；目标 aggregate value semantics 不暴露 physical alias/backing identity，普通参数 callee mutation 只修改 callee value，不需要 ServiceContract 表达 caller writeback；只有显式 `InOut` 表示 caller-writable 且 service boundary 一律非法；compiler 可为 Package Local ABI/优化追踪 physical provenance/move/share/write facts，但 boundary eligibility 不能把 COW backing alias 当语言可观察 identity；native/resource/interface capability 可否跨界由显式 type/value plan 与 adapter contract 决定，unknown target/type 独立 fail closed；本地 `any I`/native handle 跨 service 只能投影成 request-scope callback capability（ownerDeploymentBuildId/ownerRuntimeRoute/requestIdentity/interfaceOrAdapterContract/opaqueCapabilityId；生命周期到顶层 request 结束、stream 时到关闭、内部停止/owner 退出提前失效；对端只能按 contract operation 回调 owner；不能进 DB/dispatch/queue/persistent lane）。 | `runtime/boundary/src/recoverable.rs` ＋ `runtime/boundary/src/value.rs` | missing | Phase 6A / 6B | TBD（Phase 0 审计） |
| R-134 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | Effect 与 boundary eligibility：compiler 执行 sound may-analysis 至少追踪 aggregate value 的 move/share/write 与显式 InOut loan、返回/throw payload 的 logical type/value plan、caller value 是否 escape 到 capture/callback/stream/dispatch/DB/native-external target、callback/native adapter requirement、suspension summary 与 unknown call/effect；分析允许保守拒绝不允许漏掉 boundary-visible 行为；普通 callee-local collection/record mutation 不再使 callable 无法成为 service operation；第一版不新增 local/remote 源码修饰符；Service call lowering 只生成 ServiceCallRef 和 contract value plan refs，deployment image linker 生成 service dependency slot/thunk，slot 保留 contract 坐标调用时解析 provider build；consumer 不依赖 provider PackageLocalAbi。 | `compiler/source` ＋ `runtime/linker` | missing | Phase 2 / 3B | TBD（Phase 0 审计） |
| R-135 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | §11 Baked Config 与 platform policy：发布时 tooling 读取三层 config 递归 overlay（mapping 递归合并/scalar-sequence 整体替换/null tombstone），用 exact package closure 校验 unknown Package ID/required path/类型并为每个 exact Package build 生成只读 ConfigView；验证后的 config graph 写入 immutable ServiceDeployment-owned `BakedConfigPayload`，deployment 只保存不可替换 opaque protected ref，ref 进入 buildId；`BakedConfigPayloadRef = KeyedIdentity(storeDomain.configIdentityKey, skiff-baked-config-payload-v1 || canonicalBakedConfigBytes)`，`KeyedIdentity` 必须是带版本 domain separation 的 cryptographic PRF/MAC（如 HMAC-SHA-256），不能退化为 unkeyed content hash 或可逆编码；同 domain 同 canonical payload 同 ref，不同 domain 不同 ref；不知道 domain key 不能离线枚举；canonical bytes 与 secret 明文不得进入公开 preimage/log/receipt/control frame；密文 nonce 不参与 ref；重复 put 重读解密验证后幂等复用；store 保证 immutable + authenticated decryption；runtime 按 buildId 加载只读 deployment 固定 ref，missing/替换/无法解密/完整性失败 → 整个 image load fail closed；POSIX secret 源 0600 非 symlink，store/temp file owner-only；配置值不得进入 PackageArtifact/ServiceContract/release pointer key/receipt/control frame/log。 | `artifact-identity` ＋ tooling | missing | Phase 3A | TBD（Phase 0 审计） |
| R-136 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | §12 DeploymentExecutionImage 与扩容：loader 必须先 pre-link structural validation 再 link 再 post-link semantic verification（完整契约见 bytecode-vm.md）；image 只对应一个 exact buildId，不得因 load 时 provider pointer 不同生成不同内容；Package requirement 在 image 内绑定 immutable PackageArtifact（升级必须重新 build/link deployment）；Service requirement 只绑定 serviceId + exact version + expectedProtocolIdentity，每次 boundary invocation 解析 pointer pin provider build；pointer 更新不覆盖 immutable artifact 不改变已 pin invocation；多个 runtime replica 是可互换 worker（各自 lazy-load、loaded set/eviction 独立、in-flight 以 strong owner pin 保活）；扩容只增加 worker 不要求全量预载或 replica 同步；ReleaseBundle 只含 deploymentRefs + verificationReceipts，不含 linked image/address/ConfigView/execution context，不进 Router↔Runtime frame。 | `runtime/loader/src/deployment.rs` ＋ `runtime/linker` | missing | Phase 3A / 3B | TBD（Phase 0 审计） |
| R-137 | 576bbd31 | doc/architecture/package-service-contract-deployment.md | §13/§14 registry/publish 与 fail-closed 更新：registry 分别存三类 immutable artifact 与可选 bundle；`BakedConfigPayload` 是 deployment-owned protected 子记录不成为第五类 runtime artifact；immutable write、artifact graph materialization ordering、`(profile, serviceId, version) -> deployment buildId` pointer、rollback 与 preload 机械契约由 runtime-lazy-load-deployment.md 定义；registry deploy 只调用 canonical pointer writer 更新一个 key，rollback 指回已验证旧 buildId；load timeout 是 runtime/operator 本地预算与 business request timeout 分离；publish 是操作不产生 Publication 对象，workflow/bundle 不能改变单键 pointer 的原子性边界；fail-closed 清单更新（InOut boundary unavailable、pointer 缺失/provider protocol 不匹配/build 无法 load、shared package call site 全局绑定绕过 owner、linker 在 structural validation 前读 artifact-controlled index）。 | tooling ＋ `runtime/loader` | missing | Phase 3A / 3B | TBD（Phase 0 审计） |
| R-138 | 576bbd31 | doc/architecture/profile-stack-deployment.md（D） | Retirement：profile 激活/`skiff stack` 部署文档删除；activation_state 仓库、coordinator prepare/commit/abort、epoch/generation lease、config snapshot 独立提交等概念已下线（M4），由 runtime-lazy-load-deployment.md 的指针表 + buildId 懒加载取代；审计：activation_state/coordinator/epoch/lease 相关代码与 `assembly.activation` 帧族、`/__skiff/activate-assembly` 退役路径不得残留。 | `runtime/loader` ＋ `router` | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-139 | 576bbd31 | doc/architecture/recoverable-value.md | 与 bytecode VM 的边界：recoverable codec 只消费 logical typed value snapshot 并遍历为 acyclic value tree，不消费 VM physical representation；`ValueSlot` bytes、Request/Actor/Constant heap handle、handle generation、GC address、unique/share/edit token、COW backing identity、fiber/pc/resume id 与 transient root 不进入 envelope；两个 logical snapshot 共享 physical backing 时分别编码成等价 acyclic tree，decode 不重建 physical alias；`dup`/share transition/path COW 是 VM 内部事实不改变 recoverable closure 判定；`ConstRef` encode 为其 logical value + 必要 stable code/type identity，decode 在当前 deployment owner 中 materialize fresh value；`InOut` 永远不是 recoverable value（compiler 在 boundary plan 静态拒绝，codec 收到动态路径的 InOut loan 必须 fail closed）；request-scope callback capability/restricted callback closure/live resource/PendingOperation/UnwindState 不可恢复；VM 替换 allocator/collector/slot layout/collection representation 不升级 recoverable schema。 | `runtime/boundary/src/recoverable.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-140 | 576bbd31 | doc/architecture/recoverable-value.md | 恢复语义：普通 aggregate 遵守 value semantics，恢复结果是同一 logical typed snapshot，不存在可观察 raw reference identity 或 mutable alias；heap 地址、native handle 地址、in-memory object identity、COW backing identity 与物理共享拓扑都不要求一致；目标用户 value graph 不支持可观察 cycle，codec 只接受能展开为 acyclic logical value tree 的 snapshot，损坏 state/native adapter/未验证输入暴露 cycle 时 encode/decode 都 fail closed 不生成 back-reference node。 | `runtime/boundary/src/recoverable.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-141 | 576bbd31 | doc/architecture/recoverable-value.md | 当前 execution context 恢复：decode 所在 request 已由 `DeploymentExecutionContext` 钉住 exact deployment buildId 的 immutable image；codec 只使用该 image 的 linked program/method table registry/expected type plan 解释 `LocalConcreteRestoreKey`，不从 ambient/latest release 重新选 owner；exact buildId 是本次 execution owner 事实不是 durable local behavior code identity，envelope 不保存它；恢复路径不存在 `RuntimeAssembly`/assembly-activation generation/ambient active set/跨 image lookup；恢复必须同时满足唯一找到 (owner, concrete_type_identity)、concrete 仍按 expected plan 恢复、any I 唯一给出 interface/projection。 | `runtime/boundary/src/recoverable.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-142 | 576bbd31 | doc/architecture/recoverable-value.md | `carrier = Remote` 与 callback capability 不可恢复：`Remote` 是正向 service-call public instance 的 request-scoped capability，进入 owner-internal DB/dispatch/queue/persistent payload 或显式 envelope slot 时 encode 点 fail closed；不保存 dependency_ref/public_instance_key/operation table；envelope schema 不含 `Remote` variant（`InterfaceValueState` 只含 self_node）；decoder 收到未知/伪造 remote carrier variant 必须 `recoverable_state_invalid` fail closed 不尝试重建 operation table；任何 trust boundary 下 Remote/callback capability 都不可恢复；ToolProvider 只有 `carrier = Local` 且 self payload/identity 全可恢复才能跨边界；agent dispatch payload 不保存 provider array，只传稳定 id（threadId/runId），worker 从 `AgentRun.runtimeBindings` 读取 run 生命周期冻结的 provider array。 | `runtime/boundary/src/recoverable.rs` ＋ `compiler` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-143 | 576bbd31 | doc/architecture/release-registry.md（D） | Retirement：release/registry 架构文档删除；其概念（immutable records、typed pointer、rollback、audit、production workflow）由 runtime-lazy-load-deployment.md（指针表/lazy load/rollback 机械契约）与 package-service-contract-deployment.md（artifact 对象/identity）接管；审计：`RuntimeAssembly` record/pointer、assembly admission/prepare/commit 相关代码删除，`(profile, serviceId, version) -> buildId` 单键指针保留。 | tooling ＋ `runtime/loader` | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-144 | 576bbd31 | doc/architecture/router-rust.md | Router scope：`skiff-router` 负责 external ingress、release resolution、Runtime selection、request/stream correlation、WebSocket connection/broker 与 Actor routing；Runtime 执行用户代码并按 buildId lazy-load；Router 不启动 Runtime/MongoDB/telemetry、不解析 Package executable、不持有 deployment 执行状态；Router 与 Runtime 是两个独立 Rust 进程不共享 mutable state；external request 必须带受信 `x-skiff-service`/`x-skiff-version`，Router 严格解析 `(profile, serviceId, version) -> buildId` pointer 再在该 deployment 内解析 gateway entry，缺 selector/pointer/entry/eligible Runtime 都 fail closed。 | `router` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-145 | 576bbd31 | doc/architecture/router-rust.md | Connection bootstrap：Runtime 主动连接后 Router 恰好发送一次连接级 bootstrap（artifactsPath/serviceDb.mongoUrl/http.maxResponseBytes/transport-schema capabilities）；同一连接中缺失/重复冲突/变更都 fail closed；Runtime 随后通告 RuntimeReplicaId/loadedBuildIds/lazyLoadCapability+artifactStoreIdentity/transport capabilities；loaded set 是 placement hint 不是 release truth；Router 不等待全体 replica 加载某 build 也不等待多 replica 确认；preload 只能 fire-and-forget。 | `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-146 | 576bbd31 | doc/architecture/router-rust.md | State owners：Router 业务 mutable state 穷尽为 pointer/session/routing/actor 四个 domain（`ReleasePointerIndex`/`RuntimeSessionDirectory`/`RequestRoutingState`/`ActorRoutingState`），每事实唯一 owner；禁止万能 `RouterCore` 或协调器；RequestDispatcher/ClientConnectionIndex/WebSocketRequestBroker 只是 RequestRoutingState 内互斥记录种类；Actor registry/instance broker/invocation relay/owner-control broker/expiry scheduler 只是 ActorRoutingState 内部结构；RouterSupervisor 只负责 process config/construction/listener-task join/shutdown 不成为第五个业务 state domain；health 只读投影不能反向修改 owner。 | `router` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-147 | 576bbd31 | doc/architecture/router-rust.md | Routing/pinning invariants：一次新 dispatch 按序捕获 trusted service/version → ReleasePointerIndex exact buildId → RequestRoutingState exact gateway entry identity → eligible Runtime session+capacity permit → immutable `DispatchOwnerPin(buildId, deployment identity, session incarnation)`；candidate 是已注册该 buildId 或声明同一 artifact store lazy-load 能力的 session；pointer 在 dispatch 后变化不迁移 request/stream/connection，新 dispatch 重新解析；Runtime load 失败以该 request 明确 platform error 收敛，Router 不改投另一 build；session replacement 先 cancel/close old incarnation 再安装 new，old finalizer 不能删除 replacement 记录；pending 同时持有 exact owner pin/session lease/permit 且 terminal 恰好释放一次；同进程 service child 由 Runtime 自己 boundary scheduler 处理，Router 不把 service call 降级为 Package call；WebSocket upgrade 固定 exact build/gateway entry/`ClientSocketIncarnation`，JSON-RPC response 精确匹配 connection/incarnation/direction/profile/transport id；Actor routing 用独立 `ActorIncarnationFence`，实例 get/create 只改变 ActorRoutingState 不能携带或重建 ambient multi-service release state。 | `router/src/dispatch/` ＋ `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-148 | 576bbd31 | doc/architecture/router-rust.md | Model ownership/limits/rollback：Router↔Runtime wire 归 `skiff-runtime-transport`；artifact/identity 归 artifact-model/artifact-identity（Router 只消费 strict routing view 不解析 code）；release pointer 归 runtime-lazy-load-deployment.md 定义的 store；`skiff-router` 不得依赖 runtime evaluator/VM/host execution，Runtime 不依赖 Router 实现 crate；unknown nested wire/artifact field/schema mismatch/owner mismatch/cross-build substitution 都 fail closed 无 dual-read 或 display-name fallback；`runtime.maxConcurrency` 按 Runtime WebSocket session 限制 ordinary pending request 满载立即 overload 不排队，Actor/control frame 不计；`/__router/health` 是 pointer/session capability/loaded build/loop-risk 的只读 projection；service rollback 只原子把 release pointer 指回已验证旧 buildId；Router binary rollback 是 process release 操作（停流量→shutdown→启动 immutable binary→等 Runtime reconnect→HTTP/WS/Actor/chat smoke）。 | `router` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-149 | 576bbd31 | doc/architecture/runtime-compiler-shared-artifact-types.md（D） | Retirement：compiler/runtime 共享 artifact types 文档删除；其概念（artifact-model/artifact-identity owner 分层、linked overlay、Package 两身份域、generation 严格读取）被 compiler-package-pipeline.md 与 package-service-contract-deployment.md 接管；审计：artifact-model/artifact-identity 边界、`RuntimeAssembly`/`RuntimeConfigSnapshot` DTO 与 reader/writer 删除，不得保留旧 Unit/common aggregate 的兼容读取。 | `artifact-model` ＋ `artifact-identity` | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-150 | 576bbd31 | doc/architecture/runtime-deployment-topology.md（D） | Retirement：runtime deployment topology 文档删除；assembly-per-profile、activation generation、`ActiveAssemblyContextSet`/`ActivationExecutionContextRebinder`、cold activation lane、artifact filesystem bootstrap、prepare budget 等概念下线或被 runtime-lazy-load-deployment.md/router-rust.md 取代；审计：assembly/activation 相关 Runtime/Router 代码与 `/__skiff/activate-assembly` 路径删除，`activation.prepareTimeoutMs` 等配置删除。 | `runtime/host` ＋ `router` | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-151 | 576bbd31 | doc/architecture/runtime-layered-crate-architecture.md（D） | Retirement：runtime layered crate 架构文档删除；其分层目标（boundary/native-contract/linker/loader 依赖方向、`BoundaryConversionPlan`、`NativeCallPlan`、crate promotion rule）大部分已实现或并入 bytecode-vm.md 的新内部契约；审计：runtime crate 依赖 DAG 按新模型校验（scripts/check-runtime-crate-dag.mjs），旧概念（`RuntimeProgram`、activation crate 层、`EvalRuntimeProgram` projection）删除/合并，serde_json::Value 策略（typed pipeline 不得以 Value 为万能中间表示）保持。 | `runtime` 各 crate ＋ `scripts/check-runtime-crate-dag.mjs` | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-152 | 576bbd31 | doc/architecture/runtime-lazy-load-deployment.md | 四条不变式：产物只增不换（公开 artifact 相同内容相同全局 identity，含 protected config ref 的 deployment 在同一 store security domain 内确定性重构建为同一 buildId）；唯一可变状态是 `(profile, serviceId, version) → buildId` 小指针表（单键原子更新，多版本并存，不聚合成跨 service 运行对象）；runtime 无协调状态（buildId 唯一加载单位，内存有直接执行、没有从 artifact 目录懒加载、加载不到就是错误，不感知人类版本坐标不参与跨 runtime 切换协议）；路由 fail closed（router 解析指针后只派发给已加载该 buildId 或具备懒加载能力的 runtime）。 | `runtime/loader` ＋ `router` | existing-needs-proof | Phase 3A | 文档断言：runtime-lazy-load-deployment.md 被删的迁移路径（已完成）节（M1–M4 已落地）；最终证明 TBD（Phase 0 审计） |
| R-153 | 576bbd31 | doc/architecture/runtime-lazy-load-deployment.md | 部署记录：`DeploymentExecutionImage` 是 exact deployment buildId 的 immutable runtime image，只闭合该 deployment 的 package-direct code/type/const/capability；是唯一运行执行单位，跨 service provider 每次 boundary invocation 另行解析并 pin 不进入 consumer image identity；deployment 记录以不可替换 `BakedConfigPayloadRef` 冻结 protected 配置，指向 package 闭包与 file-ir，保存 service dependency contract slot，不保存 provider build 或 runtime address。 | `runtime/loader/src/deployment.rs` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-154 | 576bbd31 | doc/architecture/runtime-lazy-load-deployment.md | Immutable store/pointer/audit：registry 分别存 PackageArtifact 及子记录、ServiceContract、ServiceDeployment 与可选 bundle，无共同 kind/父 DTO；写入按 canonical bytes 算 identity，临时文件+完整 flush/rename 后重读校验；record 缺失/hash-owner 不符/闭包不完整 fail closed；reader 不能观察半写入 record 也不能从 source revision 补齐 runtime 事实；pointer 更新单键原子（rename+lock 或等价 CAS），publish 必须先使目标 deployment 及闭包完整可读再更新 pointer；每次 production 变更保存 append-only history（old/new target、actor、时间、原因、toolchain/provenance receipt），audit 不成为 runtime fallback；rollback 只把同一 key 指回仍可验证的旧 buildId 不修改历史 record；Package coordinate pointer 只能供 compiler/tooling 解析依赖，不能被 Runtime 用于选择已构建 image 内的 PackageArtifact；source revision 只服务可信重编译/审计/复现不是 Runtime 加载对象；Router/Runtime 只消费已 materialize 的 immutable store 与 release pointer，不反向调用 registry 业务 service。 | tooling ＋ `runtime/loader` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-155 | 576bbd31 | doc/architecture/runtime-lazy-load-deployment.md | 懒加载协议与配置归属：runtime 收到 buildId → 内存有已验证 image 直接执行；没有 → 进 per-buildId 临界区，持锁者按内容寻址读 deployment 记录/package 闭包/file-ir，先 pre-link structural validation 再 link relocation/constant heap 再 post-link semantic verification 并构建 image；加载成功注册进已加载集合放行等待请求，失败/超时快速失败；同一 buildId 不得因 load 期间 service pointer 取值不同生成不同 image；service dependency slot 执行时解析 provider pointer；已加载集合是 runtime 本地 cache 可增长可按本地策略逐出未被 owner pin 的 image（逐出后回同一懒加载路径），cache 变化不修改 release pointer 不要求其它 replica 同步；服务配置发布时冻结进 deployment-owned BakedConfigPayload 随 buildId 懒加载；Router 配置唯一 artifactsPath/serviceDb.mongoUrl，Runtime 主动连接后 Router 发送一次连接级 bootstrap，缺失/重复/变更 fail closed。 | `runtime/loader` ＋ `router/src/session/` | missing | Phase 3A / 3B | TBD（Phase 0 审计） |
| R-156 | 576bbd31 | doc/architecture/runtime-value-layout-and-type-erasure.md（D） | Retirement：runtime value layout/type erasure 文档删除；其原则（physical runtime value 不携带 source nominal identity、request-scope memory、expected-type driven 边界编解码、exception envelope 显式 identity）被 bytecode-vm.md §6（compact ValueSlot/TypeTagId/handle discipline）与 §15（boundary/recoverable）取代；审计：`RuntimeValue`/`HeapNode`/`__skiffType`/`TAG_REPRESENTATION`/`DynamicReceiver`/RuntimeObject source-name shape 字段等旧实现概念按被删文档的 audit 清单（rg 目标）删除或保留为 fail-closed legacy fixture 拒绝路径。 | `runtime/model` ＋ `runtime/eval` | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-157 | 576bbd31 | doc/architecture/tail-call-execution.md | 目标实现服务当前 request 钉住的 exact deployment buildId 的 `DeploymentExecutionImage`；树遍历 evaluator 迁移期间仍可使用本文件记录的 `Flow::TailCall` trampoline（`runtime/eval` 在可传播到 callable exit 的 `Return` 处识别 `LinkedExprIr::Call` 与 exact `LinkedCallTarget::Executable`，`runtime/eval::program_execution` 拥有唯一 prepared tail frame 与迭代 trampoline，implicit/explicit self 收敛到同一 loop），但 production cutover 后由 bytecode `tail_call_local` 替代；不保留 legacy/test projection 或 fallback；不增加语言关键字/annotation/manifest 字段/环境旋钮/用户配置，不依赖 tokio::spawn 拆 task。 | `runtime/eval/src/program_execution.rs` | existing-needs-proof | Phase 4 / 8 | 文档断言：tail-call-execution.md Tree-evaluator migration control contract（Flow::TailCall trampoline 在 runtime/eval）；doc/implementation/tail-call-execution/ 有既有分解；最终证明 TBD（Phase 0 审计） |
| R-158 | 576bbd31 | doc/architecture/tail-call-execution.md | Bytecode 组合（四部分）：source/lowering 按 reference 识别 eligible direct-return call 并保留 exact relocation 与 return plan facts；canonical artifact ISA 新增 semantic opcode `tail_call_local`，emitter 为 eligible edge 发射该 instruction 与 exact target relocation（不是可漂移的 boolean marker）；pre-link validator 验证 opcode/relocation 结构，post-link verifier 证明 exact-local target、return plan、self/generic substitution、`NoPending`/`InOut` 与 cleanup-region eligibility；VM 在同一 dispatch loop 中以单一 commit 替换当前 frame/value segment；树遍历阶段不新增 File IR `tail` 字段/metadata convention/SCC annotation；`tail_call_local` 的 relocation 只能在当前 exact buildId 的 image 内解析成 exact executable target，frame replacement 不得改变 DeploymentOwnerIdentity 或跨 service/deployment 边界；两代实现都不得只优化 self symbol，不得用 tokio::spawn 或每 hop heap continuation 模拟 TCO；排除项：PackageDirect/interface const receiver/service/Actor dispatch/callback capability/native/builtin/stream defer-dispatch-emit 不进入第一版 trampoline；generic type args 必须在 caller env 销毁前通过既有 call_type_substitutions 实例化，tail replacement 不能重新推断 receiver 也不能丢失 self carrier 的 heap/catch identity。 | `compiler/emission` ＋ `artifact-model` ＋ `runtime/linker` ＋ `runtime/vm` | missing | Phase 2 / 3B / 4 | TBD（Phase 0 审计） |
| R-159 | 576bbd31 | doc/architecture/tail-call-execution.md | Bytecode frame replacement safety：`tail_call_local` 是已验证 artifact 的 semantic instruction 不是 runtime 探测后可回退的 optimization；post-link verifier 必须证明：target 在同 exact buildId image 内且 arity/parameter-return plan/self/generic substitution/Package Local ABI 精确匹配、opcode 处于可直接退出 callable 的 region depth（无未完成 cleanup/catch/timeout/transaction/scheduler owner、不在 unwind path）、frame 中没有必须由被删除 slot 继续拥有的 callback capture/resource guard/transient root/pending-resume state（VM 只能在 fiber Runnable、PendingOperation=None、UnwindState=None 时 commit replacement）、普通 aggregate argument 按 value semantics 做 move/share snapshot（physical backing 可共享但 callee 不能因 frame replacement 获得可观察 mutable alias 或指向已删 slot 的 raw reference）；VM 在 caller frame 完整存活时按源码顺序求值每个 argument 一次放入 rooted prepared segment，任何失败保留 caller frame 可 unwind 不留部分新 frame；commit 步骤（保留 caller continuation/return destination/owner → loan 结束或转移 → 截断旧 segment 安装 callee frame → 切 pc），中间不暴露 half-replaced state；无法证明时不发射 tail_call_local（emitter 发 call_local+return 或 verifier fail closed），runtime 不能把已验证的 tail_call_local 临时降级成普通 call。 | `runtime/linker`（verifier）＋ `runtime/vm` | missing | Phase 3B / 4 | TBD（Phase 0 审计） |
| R-160 | 576bbd31 | doc/architecture/tail-call-execution.md | `NoPending`/`InOut` 与 tail：任何拥有 `InOut` 形参的 callable 必须通过 transitive `NoPending` 验证（该 callable 可达的 tail_call_local/argument evaluation/target body 不得到达 pending-capable instruction，不能用 runtime 这次恰好 Ready 替代 proof）；target 含 InOut 形参时 verifier 必须同时证明 exact local executable、Local ABI NoPending、actual 是 exclusive writable var path、loan 不逃逸到 callback/resource/concurrent sibling；caller 已有 incoming loan 只能在 tail edge 结束或将同一 writable-path descriptor 与 loan token 线性转移给 callee，不允许 copy loan 或同时保留两个 writer；以将被截断的 caller local slot 为 base 的新 InOut 实参不能在首版进入 tail_call_local（emitter 保留普通 call）；无法证明的 edge 不得发射 tail_call_local，损坏 artifact 在 post-link 拒绝。 | `runtime/linker`（verifier）＋ `compiler/emission` | missing | Phase 3B / 4 | TBD（Phase 0 审计） |
| R-161 | 576bbd31 | doc/architecture/tail-call-execution.md | 验证与迁移：MAX_PROGRAM_CALL_DEPTH=128 作为 non-tail native-stack crash fuse 保留（语义收窄为 active non-tail program depth）；tail chain 诊断空间常量有界（被消除的 tail edge 不增长 exception stack）；树遍历 evaluator 存活期间 canonical 与 test path 共享同一 linked evaluator/trampoline 不增加 test-only 识别；cutover 后 PackageArtifact 保存 relocatable tail_call_local，exact buildId loader/linker/verifier 产生唯一可执行 image，malformed/不满足 eligibility 的 opcode fail closed 不能在 runtime 降级成另一 projection，旧 `RuntimeExecutionProjection`/`EvalProgramProjection` 与 tree trampoline 一起删除，语义改变的 aggregate value/top-level const/InOut case 不以旧 evaluator 作等价 oracle；runtime positive 覆盖 direct self/same-file/cross-module mutual/generic-impl self/branch return/ordered-single argument evaluation；至少一个 runtime test 在 1 MiB Tokio worker stack 上完成 100,000 次 tail hop，至少一个 canonical source fixture 经 source → compiler → bytecode artifact → deployment load/link/verify → runtime 路径覆盖 >32 层递归。 | `runtime/eval` ＋ `runtime/vm` ＋ test | missing | Phase 4 / 8 | TBD（Phase 0 审计） |
| R-162 | 576bbd31 | doc/architecture/test-runner-runtime-isolation.md | 隔离边界：普通 skiff test/canonical 套件/实现测试都不借用开发者常驻 Router/Runtime/artifact root/build root；最外层 Node host orchestrator 拥有临时 instance（Router HTTP/control 用 46000–46999 租约保护动态端口；artifact store/build root/runtime home/pid/log/process config 在同一临时 workspace；Mongo 可连本机 27017 但不启动；binary 来自当前 checkout）；`run-skiff-tests.mjs` 为整个 canonical registry plan 只创建一套进程，entry 可复用进程与 immutable store 不复用 case mutable state；Router 不接受空 artifact root，supervisor 启动前 CLI 用当前 checkout 的 compiler/publisher 向临时 store 写入专用 bootstrap ServiceDeployment 并写入隔离 profile 的 release pointer；带 service/version selector 的 bootstrap HTTP 路由触发 Runtime 按 buildId 懒加载；readiness 必须同时证明 Router 可路由、Runtime session 可 dispatch、bootstrap build 已加载，health 200 不足够。 | `scripts`（test-runner） | missing | Phase 3A | TBD（Phase 0 审计） |
| R-163 | 576bbd31 | doc/architecture/test-runner-runtime-isolation.md | Runner contract：canonical registry 由 `scripts/lib/skiff-source-test-registry.mjs` 唯一声明；每 entry 走 production `skiff-test-runner` path 固定 `--deny-skips`/`--require-tests` 不传 `--live`/`--allow-network`；同 kind:test service 只编译一次 Package/读一次 config layer/resolve 一次 dependency graph；non-live case 按 relative_path 贪心装箱每 batch ≤16（单文件超限才切分），batch 只是 runner 调度和有界资源回收单位，不生成 RuntimeAssembly/RuntimeConfigSnapshot/activation generation 也不成为任何 execution authority；分批前冻结 resolved authored config 与 `skiff.test.ingressUrl` overlay；每 case 独立 synthetic service identity/ServiceContract/ServiceDeployment、含 case config 与 ingress overlay 的 deployment-owned BakedConfigPayload、独立 gateway entry/ingress selector 与 release pointer、精确 buildId/数据库 identity/request heap/effect registry/execution nonce；全部 case deployment 必须在第一次 dispatch 前完成构建、结构验证、发布与 pointer 写入；Runtime 只以每 case 精确 buildId 为 load key 懒加载 image，不接收 batch identity 不读 release pointer 重新选择 build。 | `scripts`（test-runner）＋ `runtime/loader` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-164 | 576bbd31 | doc/architecture/test-runner-runtime-isolation.md | Derived test authority：每 root test dispatch 由 Router 新建不透明 `testCaseCapability`；case identity 至少含 `(testRunId, generatedTestServiceId, buildId, testCaseCapability)`，batch 序号与 release pointer 不是 authority；capability 与精确 case buildId 共同定界 execution，任一项不能单独授权派生或替代另一项；capability 只存在 Router/Runtime Host 私有传输与注册状态，不进入 Skiff value/config/业务 effect surface；direct/recursive dispatch 与 Actor method dispatch 的 `task.submit.request` 只携带 callerRequestId 与 TaskId；Router 只从同一 Runtime WebSocket session 上仍 active 的父 request 派生 capability、精确 test deployment buildId 和 case lease，不信任 Runtime 单独提供的 capability 或未绑定 session 的 parent id；父结束/跨 session/跨 root/跨 service 派生 fail closed；派生链不可变钉住 root case 的 deployment buildId，release pointer 更新不重绑已运行 request 或 Actor，self-ingress 从 immutable deployment 读 gateway metadata 不重读 current pointer；携带 test capability 的 Actor owner 必须是派生链精确 origin Runtime id 与 connection，先建 cancellation correlation 与 derived case lease 再移交 execution，任一 admission 失败不得回报 receipt 或启动 task；root close 拒绝 late child 但 finalization 等待已 admit child 释放 lease；首版每 case 只允许一个 active self-ingress。 | `router/src/task/` ＋ `scripts` | missing | Phase 3A / 7 | TBD（Phase 0 审计） |
| R-165 | 576bbd31 | doc/architecture/test-runner-runtime-isolation.md | Runner inputs：Node host 显式注入 `SKIFF_DEV_HOME`、`SKIFF_TEST_RUNTIME_ARTIFACT_ROOT`、`SKIFF_TEST_CONTROL_URL`、`SKIFF_TEST_INGRESS_URL`、`SKIFF_TEST_ENVIRONMENT`、`SKIFF_TEST_PLATFORM_SOURCE_ROOT`；不再注入 activation URL 或 expected generation；CLI options 高于环境变量，组合不完整时在任何网络请求前 fail closed；control/ingress target 只接受带显式端口的 IPv4/DNS http:// URL，拒绝默认端口/userinfo/query/fragment，错误不回显原 URL；runner 不 fallback 127.0.0.1:4001 不从 health 推断可写 artifact root；publish/pointer API 属于 control protocol，不使用已退役 `/__skiff/activate-assembly` 或 `/__skiff/reload-artifacts`；`runtime-live` 同时启用 --deny-skips/--require-tests（SKIP 或零 result 显示 FAILED 非零退出），live target 由 release profile/control URL/ingress URL/artifact root 唯一描述，不接受或推导 base assembly/snapshot pair。 | `scripts`（test-runner） | missing | Phase 3A | TBD（Phase 0 审计） |
| R-166 | 576bbd31 | doc/reference/any-interface-value.md | 用户可见 value semantics 与执行 owner：record/Array/Map/JsonObject 及本地 any I payload 是 value semantics（装箱/赋值/普通传参/返回/container store 产生逻辑 snapshot，可 move/share/COW 共享 physical backing 但不得暴露 mutable alias）；局部 let 与普通 parameter 不可写，局部 var 及派生精确 writable path 可写，顶层 const 是 compiler-evaluated 且 deeply frozen；显式 inout 只能作为 exact Package Local ABI、NoPending concrete call 的 exclusive loan，不得出现在 interface requirement/callback/service-gateway-Actor external contract/host effect/recoverable boundary；每个执行中的 request/stream producer/callback invocation 由 exact deployment `buildId` 唯一固定到 immutable execution image；execution owner 不是 deployment activation generation 也不是全局 runtime assembly；Actor activation identity 与 transport socket generation 不替代 buildId；owner-internal boundary 的 owner（service trust domain）与 runtime execution owner 是不同维度。 | `runtime/boundary/src/value.rs` ＋ `compiler` | missing | Phase 2 / 6A | TBD（Phase 0 审计） |
| R-167 | 576bbd31 | doc/reference/any-interface-value.md | `carrier = Local` 的装箱源是当前 exact deployment buildId execution image 中的 concrete nominal value；`carrier = Remote` 是 request-scope 引用只能当前 request 内使用；callback capability 固定创建侧的 exact buildId 与当前 runtime route（unary 活到顶层 request 结束，server stream 到关闭），对端只能按 I 声明的 operation 回调该 exact build owner，不能看到 method table/本地地址/native object；callback 位置规则（顶层非泛型 any I 参数/unary 返回/server-stream item 可投影，Array/record 内嵌/泛型 fail closed）；callback capability 失效返回 `CapabilityExpired`/`CapabilityUnavailable` 不重建不 fallback；LocalConcrete restore key 不携带 buildId/artifact identity/deployment owner/package slot/source path/runtime-local type address，恢复只在当前 exact buildId 的 linked program 内查找；跨 service 只发送 sealed opaque callback capability；`inout` loan 没有 recoverable encoding。 | `runtime/boundary` ＋ `compiler` | missing | Phase 6A / 6B | TBD（Phase 0 审计） |
| R-168 | 576bbd31 | doc/reference/any-interface.md | §2.6 值语义、可写绑定与 `inout`（用户可见）：record/Array/Map/JsonObject 及 carrier=Local payload 保持 value semantics；`as I` 先求值装箱源再放入当时逻辑 snapshot；carrier=Remote 的 snapshot 复制的是不可写寻址/operation 坐标不是远程活对象指针；局部 `let` 是不可写 runtime binding，局部 `var` 可重绑且其精确 member/index path 可写；`const` 只是顶层 compiler-evaluated、request-independent、deeply frozen 常量不是第三种局部 mutable binding；普通参数（包括 any I）是 immutable value binding，callee 需要可写副本时先放入局部 var；显式 `inout` 是唯一 caller-writable loan，只允许 exact Package Local ABI 的 Package-local/package-direct concrete callable 且 compiler 与 verifier 都证明 `NoPending`；interface requirement 不得声明 `inout`，any I 间接调用不可发起或传播 inout loan；inout 不得进入 callback/service-gateway-Actor external contract/host effect/recoverable boundary。 | `compiler/source` ＋ reference 落地 | missing | Phase 2 | TBD（Phase 0 审计） |
| R-169 | 576bbd31 | doc/reference/any-interface.md | object-safety 约束补充：method requirement 的 receiver 与其它参数都不得使用 `inout`；callback capability 的所有 operation 参数与返回值依然是 boundary snapshot，不得携带 inout mode 或 caller-writable origin（不因 owner 与对端同一 runtime 而放宽）；本地 method table、callback owner 与每次远程调用进入的 provider execution 都必须属于已固定 exact deployment buildId 的 immutable execution image；不存在可代替该 owner 的 deployment activation generation 或全局 runtime assembly；Actor activation 与 transport socket generation 只是各自域内状态不参与 interface target identity。 | `compiler/lowering` ＋ `runtime/boundary` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-170 | 576bbd31 | doc/reference/api-yml.md | 含 `inout` 参数的 public callable 是合法 Package API 与 Package Local ABI，但其 projection 必须为 `Unavailable(InOutNotAllowedAtServiceBoundary)`；不得因同进程部署而改成可跨 service。 | `compiler`（projection） | missing | Phase 2 | TBD（Phase 0 审计） |
| R-171 | 576bbd31 | doc/reference/config.md | 配置发布语义：发布一个 service deployment 时 tooling 从 service root 选择至多三份配置文件；未知 Package ID 在 deployment projection 阶段失败；overlay 结果必须满足当前 deployment package closure 中每个精确 Package build 的 typed requirements；`config.require<T>` path 缺失或类型不符使 deployment build 失败，`config.optional<T>` 缺失返回 null、存在但类型不符仍使 build 失败；同一个精确 Package build 经 diamond 多次到达时同一 deployment 中只有一份配置分区和一份只读 ConfigView；dependency alias 和到达路径不参与配置 identity。 | tooling（`compiler/driver`） | missing | Phase 3A | TBD（Phase 0 审计） |
| R-172 | 576bbd31 | doc/reference/config.md | secret 与 baked config：secret source 必须是普通文件非 symlink 且 mode 精确 0600（POSIX），tooling 读取前检查并 fail closed；明文复制/暂存写完后先设 0600 再使用；受信 deployment store 目录 0700，明文或可解密 payload 文件 0600；非 POSIX 平台需等价 owner-only ACL/拒绝 link 替换并声明边界；明文不得写入 PackageArtifact/ServiceContract/release pointer/可选 release bundle/receipt/control frame/log，只允许进入 ServiceDeployment-owned protected config payload；公开 descriptor、诊断与 public content-hash preimage 不得回显配置值。 | tooling | missing | Phase 3A | TBD（Phase 0 审计） |
| R-173 | 576bbd31 | doc/reference/config.md | §4 Baked Deployment Config：`profile` 由 producer 从受信 operator 输入取得不从 source YAML/路径/ambient 推断；tooling 把 canonical Package ID 解析到 deployment package closure 中的精确 Package build，校验完整 typed requirements 后写入 immutable `BakedConfigPayload`；deployment 保存不可替换 opaque protected ref，ref 参与 buildId，secret 明文不进入公开 hash preimage；ref 必须是 store security domain 内确定性的 keyed content identity（如 HMAC）或同等幂等与 offline-guess resistance 的受信 store identity；store 必须验证 immutability 与完整性；同 canonical payload 重复发布复用已验证 ref 不因加密 nonce 改变 buildId；Runtime 只把匹配 `(deployment buildId, PackageBuild)` 的 ConfigView 交给对应执行 slot；配置变化创建新 ServiceDeployment/buildId 但不重建 PackageArtifact/ServiceContract，publish 原子更新 release pointer，rollback 指回旧 build；不存在 RuntimeConfigSnapshot、prepare/cold-recovery 配对或 activation generation。 | `artifact-identity` ＋ tooling | missing | Phase 3A | TBD（Phase 0 审计） |
| R-174 | 576bbd31 | doc/reference/db.md | image-local DB plan：Runtime 只在 exact deployment image 的完整 DB plan 已经满足后执行该 build；plan 只来自该 image 自己的 exact package closure，不合并其它 release/build 的索引声明，不运行全局 candidate generation 或 activation prepare；image load 期间平台幂等创建缺少的普通或唯一索引并复读验证同一 stable identity 的定义完全一致；更改或删除已受管索引需要显式 schema migration，不能在 image load 时静默 drop/rebuild；当前 build 未声明的已有受管索引保留以允许多 build 并存；同一 logical index identity 已存在不合容定义时 image load fail closed。 | `runtime/loader` ＋ `runtime/host`（service_db） | missing | Phase 3A | TBD（Phase 0 审计） |
| R-175 | 576bbd31 | doc/reference/db.md | db contract target 由宿主的 exact `DeploymentExecutionImage` 基于自己的 deployment package closure 作 image-local binding；一个契约在同一 image 的 deployment package closure 内必须恰好被一个实现声明覆盖（缺失/重复/解析不一致 fail closed，按 image 无条件生效）；绑定发生在 image-local link/verification；覆盖校验失败在编译或 image load 阶段 fail closed 不进入业务 catch。 | `compiler` ＋ `runtime/loader` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-176 | 576bbd31 | doc/reference/db.md | transaction 规则（reference 版）：nested transaction 不支持（compiler 拒词法可见嵌套；经 helper 动态重入时 Runtime 必须在进入内层前拒绝，不能静默折叠）；Actor method（含 create）中 transaction 是 DB-only boundary（body 可读 Actor fields 但禁止直接或经 callee 写 Actor fields，赋值与以 Actor field 为 receiver 的原地修改都算写；compiler 必须用闭合 callable effect 检查；无法证明无 field write 的 dynamic target 保守拒绝；DB abort 不回滚 Actor fields 也不为 Actor transaction 建立内存快照）；transaction body 中的普通局部绑定用 let/var，局部 const 不是语法。 | `compiler` ＋ `runtime` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-177 | 576bbd31 | doc/reference/db.md | 连接串注入：业务源码和 service.yml 不配置真实 DB 连接串；Router 通过 connection bootstrap 向 Runtime 下发 `serviceDb.mongoUrl`；业务代码不能读取连接串也不能选择任意 database；database identity 与稳定 service id 绑定，不包含 service version/build id/profile 之外的事实。 | `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-178 | 576bbd31 | doc/reference/dispatch.md | actor-method target 提交时同时冻结 exact deployment `buildId` 与 `ActorActivationSnapshot`；Runtime 只从该 build 的 immutable `DeploymentExecutionImage` 执行；提交顺序：求值 → 冻结 exact buildId/target → recoverable encode → TaskId → TaskStore durable create；execution image pin 的是 exact buildId 及其 artifact retention root 不 pin 旧 Runtime 进程，执行时按 buildId 取得/懒加载 image，不解析 latest release pointer 不 fallback 其它 build；不存在 RuntimeAssembly 或 deployment activation generation；claim CAS 要求 exact buildId retained 且可加载。 | `router/src/task/` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-179 | 576bbd31 | doc/reference/interface.md | interface method requirement 的 receiver 和其它参数都只能是普通 value parameter，不得声明 `inout`；record/Array/Map/JsonObject 作为普通参数传递逻辑 snapshot，参数 binding 及派生 member/index path 在 callee 中不可写；callee 需修改自己副本时先绑定到局部 var，该修改不反向暴露为 caller 可观察 mutable alias；`inout` 是 caller-writable exclusive loan，只允许已精确解析到 Package-local/package-direct concrete callable 的 Package Local ABI 且 compiler 与 verifier 都证明 NoPending；interface dispatch 擦除 exact concrete target 又保守 maySuspend=true，不能借 method table/callback adapter/边界 projection 绕过；普通 public const 不自动成为可调用 root（const 是顶层 compiler 求值 deep freeze 常量，不是局部 writable binding，不因公开获得 receiver identity；局部 runtime binding 用不可写 let 或可写 var）。 | `compiler` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-180 | 576bbd31 | doc/reference/interface.md | artifact admission 必须拒绝任何在 interface requirement 或其 method-table signature 中携带 `inout` mode 的产物；`inout` mode/path effect 只属于 exact concrete callable 的 Package Local ABI，不属于 interface fact/callback contract/ServiceContract；runtime 不依据 interface id + method name 动态派发，实际 call target 必须已链接成 local executable/package callable target/service ContractOperationId；这些 linked target 总是在当前 execution 已固定的 exact deployment buildId 对应的 immutable execution image 内解析；deployment activation generation 或全局 runtime assembly 不是 execution owner，不得作为查找/兼容/fallback key。 | `compiler` ＋ `runtime/linker` | missing | Phase 3B | TBD（Phase 0 审计） |
| R-181 | 576bbd31 | doc/reference/observability.md | 事件归属：service 归属用 service id/revision id/exact deployment `buildId`；runtime 归属用 `runtimeId`/`runtimeSessionId`；execution 事件上的 buildId 必须是本次执行 pin 住的 exact ServiceDeployment build id，不是 Package build id、release pointer 当前值、deployment activation identity、runtime activation identity 或 assembly generation；runtimeSessionId 表示本次物理连接的 session epoch 与稳定 runtimeId/replica identity 分开；Actor activation id 与 client socket generation 只能作为明确 Actor/socket 生命周期事件的 scoped attrs，不能代替通用执行归属。 | `runtime`（telemetry） | missing | Phase 9 | TBD（Phase 0 审计） |
| R-182 | 576bbd31 | doc/reference/observability.md | 观测面更新：runtime error 帧引用当前 deployment image 内 source id，依赖事件上 exact buildId 回查 source map；request frame 事件关联 stable target id/service revision/exact buildId/runtimeId/runtimeSessionId/trace-span；runtime 自动补充这些上下文；deployment image load/link/verify/cache/rejection、VM fiber/owner transfer、managed heap/GC 产生可按 exact buildId 与 runtimeSessionId 聚合的 metric/health 事件；禁止用 deployment activation identity/runtime activation identity/assembly generation 代替 buildId/runtimeSessionId 表达通用执行归属。 | `runtime`（telemetry） | missing | Phase 9 | TBD（Phase 0 审计） |
| R-183 | 576bbd31 | doc/reference/queue.md（D） | Retirement：queue/scheduling reference 文档删除；其 durable 调度能力边界（queue item/policy/claim/lease/timer、cancel/timeout、同步请求排队）不再作为用户可见 surface；已实现的 TaskStore/durable task 契约由 durable-task-dispatch.md + dispatch.md 承担；审计：TaskStore 相关代码保留并验证，queue 对象暴露 surface 未实现也不得新增；queue 概念（payload recoverable 底线、at-least-once、lease fencing）归入 durable-task-dispatch.md。 | `router/src/task/` ＋ `runtime` | retirement-only | Phase 8 | TBD（Phase 0 审计） |
| R-184 | 576bbd31 | doc/reference/runtime.md | 部署 owner 路由（reference 版）：Hub/router 负责 Runtime session 注册、release pointer 解析、loaded/lazy-load capability 选择、protocol/gateway identity 匹配、client session/actor binding/WS Connection 索引、in-flight request/stream 配对、internal stop/drain 路由与容量门禁；Router 先严格解析 ingress 注入的 x-skiff-service/x-skiff-version 经 release pointer 选择唯一精确 deployment buildId，再在该 deployment 内解释 method/path；Service runtime 按 exact buildId 加载、验证并缓存 immutable DeploymentExecutionImage，为每次 dispatch 创建 request frame；request frame 不包含 ambient/current release state；Router 到 Runtime 的 frame 必须携带精确 buildId 与 gateway entry identity，Runtime 只接受与该 buildId 懒加载并验证完成的 image 逐项匹配的 tuple，不从 Host/path/latest pointer/ambient registration 重新推断；同一 deployment 重复 selector、缺失/非法/歧义 header、pointer 记录不兼容、跨 build substitution 都 fail closed；该路由模型不再拥有 active assembly、activation admission 或 generation frame；旧 Host-bearing route、裸全局 ingress key、assembly/config-snapshot ref 与旧 frame 不兼容读取；可选 ReleaseBundle 只是离线发布清单不进入 request transport。 | `router` ＋ `runtime/host` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-185 | 576bbd31 | doc/reference/runtime.md | HTTP limits：Router operator 配置必须给出正 safe integer `http.maxRequestBytes` 与 `http.maxResponseBytes`，没有 per-service override 或隐藏默认值；Router 在读取完整 request body 前限制前者；Runtime 按 bootstrap 下发的后者尽早停止过大 response，Router 在 external boundary 再次校验；HTTP stream 按同一 response 生命周期累计不能拆 chunk 绕过；WebSocket frame 使用自己的独立 limit。 | `router/src/http/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-186 | 576bbd31 | doc/reference/runtime.md | §5 request heap 与 value semantics：`Array<T>`/`Map<K,V>`/`JsonObject`/record 是 aggregate value，赋值/普通传参/返回/container store 产生当时内容的 logical snapshot，后续从任一 writable binding 修改不影响其它 snapshot；实现可 move unique backing 或 share transition+path COW，用户程序不能观察 physical handle/backing identity；局部 `let` 初始化与普通 parameter binding 取得 logical snapshot 且是 immutable runtime binding，派生 field/index path 不可写；aggregate writable access-path root 只来自三类：局部 var、当前有效的 InOut loan、Actor method 中允许直接写入的 self.field member/index path；顶层 const 位于 immutable ConstantHeap 是 request-independent deeply frozen 值，放入 var 后第一次写入创建 request-owned COW node 不改变 constant graph；普通 function/Package 参数与返回值是 logical snapshot 不携带 caller-writable origin；只有静态解析到 exact Package-local/package-direct concrete callable 的显式 inout 参数是短期 exclusive write-through loan，callee 必须经 verifier 证明 NoPending，inout 只进入 Package Local ABI，ordinary throw 不回滚已执行写入；每 request owner（含 service boundary 创建的 provider owner）创建 fresh request-local managed heap，collector 只由 allocation pressure 触发；opaque resource/transaction/stream source 与其它 native handle 是 identity-bearing capability，不因 aggregate value semantics 自动获得 copy/COW；heap handle 是 request-local id 不是跨 request/artifact/service 的 ABI。 | `runtime/model/src/value.rs` ＋ `compiler` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-187 | 576bbd31 | doc/reference/runtime.md | cycle 与 map 语义：目标 value graph 不支持用户可见 cycle，会形成 cycle 的 mutation/materialize/wire payload 必须 fail closed；nested mutation 沿 writable path 检查每个 node 的 share state，只复制需要分离的 path，不允许只检查最外层 collection 而泄漏嵌套 alias；`Map.keys()` 返回 Array 快照值，放入 var 后修改不影响原 map；`for key in map`/`for key, value in map` 在循环开始时读快照；map key 快照顺序是 canonical map key order（string 或 string representation，按 canonical string payload 的 UTF-8 字节序升序）不是插入顺序。 | `runtime/model/src/value.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-188 | 576bbd31 | doc/reference/runtime.md | `concurrent` lane 规则更新：`concurrent` block 词法作用域，只有直属 `let` declaration lane 的结果能被后续 sibling lane 读取（读取后方声明是 forward reference）；`var` 在 concurrent surface 直属位置非法，嵌套 block 与 serial 内声明不跨 sibling 可见；compiler 为每个 block 建立 lane DAG，lane B 读取 lane A 的 sibling-visible let 则 A 必须先于 B 完成，传入 B 的 aggregate 是 A 结果的 logical snapshot；sibling lane 禁止写入从 concurrent 外层捕获的 var 或发起 inout 调用（即使静态路径不同字段），lane-local var 可在该 lane 内 mutation；普通 aggregate snapshot 不携带共享 caller-writable reference，identity-bearing resource 仍按 effect/conflict-key 检查跨 lane 冲突；serial 只收束顺序不绕过外层 writable-place 限制；concurrent 完成前所有已启动 lane 必须 normal exit，错误/超时/internal stop 获胜后未启动 lane 不启动、运行中 lane 结构化停止，已提交外部副作用不回滚。 | `compiler` ＋ `runtime/vm` | missing | Phase 5 / 6B | TBD（Phase 0 审计） |
| R-189 | 576bbd31 | doc/reference/runtime.md | service call 的 deployment owner（reference 版）：同一进程的 service call 仍跨 service boundary；每次调用开始时 boundary resolver 按依赖中冻结的 serviceId+exact version+expected protocol identity 解析当前 release pointer 取得并 pin 精确 provider build；进入 provider 时 Runtime 一次性切换到该 build 的 deployment owner：provider 看到自己的 Package 配置/service DB/file/actor/dispatch/WebSocket/telemetry/service dependency；deadline/internal stop/time source/request lifecycle/trace-error/transport request identity/stream lifecycle/test effect/heap limits 仍属原 request；provider 使用 fresh request heap，参数/返回/错误/callback payload/stream item 按 ServiceContract materialize 不能共享 caller heap 引用；caller call frame/writable place/InOut loan/Actor execution frame 不进入 provider；内部 actor 句柄已有显式 owner 不因 service owner 切换改写；pointer 缺失/protocol 不匹配/provider build 无法加载/目标 owner 不可用时调用在 provider 执行前失败，不得按 display name/ambient service/其它 version 猜测；provider image 已同进程时 scheduler 用 child fiber 同步执行，否则走 runtime transport，两条路径都使用 fresh provider heap 和相同 boundary plan，只有解析/transport/child 执行真实等待时才产生 Pending；普通 continuation 不切换 owner，只有进入 service provider 及调用 request-scope callback capability 时发生原子切换；service 返回 stream 时 stream 继续 pin 创建它的 provider build，release pointer 更新只影响后续新调用。 | `runtime/boundary` ＋ `runtime/vm` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-190 | 576bbd31 | doc/reference/runtime.md | Runtime 连接并发门禁：`runtime.maxConcurrency` 是 router.yml runtime 段 required 正 safe integer，Router 按每条 Runtime WebSocket 连接统一限制所有普通 pending request（Actor/control frame 不计）；满载立即 overload 不排队不重试不创建新 pending；多条 Runtime 连接分别应用同一配置各自独立计数；未绑定请求可选择另一条有容量连接，已钉死连接或 deployment build 的请求不能为绕过容量迁移；Service 源码/service.yml/config 文件/ServiceDeployment/可选 release bundle 都不能声明、复制或覆盖并发上限；lifecycle 配置面非法，旧 maxConcurrency/idleTimeoutMs 已删除。 | `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-191 | 576bbd31 | doc/reference/runtime.md | Runtime identities 与 WebSocket pin：gateway entry identity 变化不改变 service protocol identity，只替换实现而外部协议不变时只改变 deployment buildId；HTTP dispatch 用 gateway entry identity 做匹配与观测；WebSocket Connection 绑定 connection protocol identity 与 exact deployment build；outbound response 必须精确匹配原 connection、socket generation 与 transport request id，inbound method 也在同一 pinned build 中解析 gateway entry；schema-changing 发布后旧 Connection 继续使用已 pin build 直到 drain/断开；Router 中专用 WebSocket request broker 拥有编码无关的 request identity、pending 生命周期与 connection/socket-generation 归属，业务 method/params/result/error data 保持 opaque；`std.websocket` send 保持非挂起，requestJsonToConnection 等待时挂起并可被内部停止/deadline 终止（TimeoutError 可捕获，ancestor 内部停止不可捕获）；deployment image lazy-load/preload 与 WebSocket connection drain 使用各自 operator-owned lifecycle budget，不能继承或覆盖 requestTimeoutMs 或用户 timeout，Skiff 没有 assembly prepare/commit/abort 或 generation release 事务。 | `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-192 | 576bbd31 | doc/reference/runtime.md | effect metadata 更新：`config.require`/`config.optional` 读取当前精确 deployment 中为 Package slot 烘焙的局部 ConfigView，是本地只读访问不是外部 I/O；Array/Map/scalar receiver 方法只产生 local read/write effect，mutation 按 writable place 或 inout path 参与并发检查，pure static transform 只读输入 snapshot；std.json encode/decode 是 boundary codec helper 不访问外部系统；non-goals 更新：任何把 request-local 状态保存进 deployment-scoped/process-global mutable singleton 的机制都禁止。 | `compiler` ＋ `runtime` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-193 | 576bbd31 | doc/reference/service-yml.md | deployment 语义（reference 版）：三种 config 文件属于 service root，普通 Package 不能自带 profile 配置；dependency Package 的值写在宿主 service 同一文件中以 canonical ID 为 key，完整 shape 与 baked-deployment 语义见 config.md；Tooling 生成 ServiceContract 和 ServiceDeployment，配置作为 deployment-owned protected payload 冻结，不存在 developer-authored 或 runtime-active assembly；只改变 serviceCalls 会改变 ServiceContract/ServiceProtocolIdentity 但不改变 PackageArtifact；HTTP/WebSocket 文件变化不改变 ServiceContract；配置文件变化创建新的 ServiceDeployment/buildId 但不改变 PackageArtifact 或 ServiceContract。 | tooling ＋ `artifact-model` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-194 | 576bbd31 | doc/reference/service-yml.md | WebSocket upgrade 与 HTTP 使用相同 service 选择阶段：Router 严格解析 x-skiff-service/x-skiff-version 选择精确 deployment，再按 `(websocket, path)` 选择 entry 并把 deployment `buildId` 固定到 socket；peer request 按 `(websocket entry id, jsonrpc-2.0-text, method)` 在 socket pin 住的 deployment build 中路由；HTTP Host 不参与 upgrade route 选择；peer disconnect 后 runtime 内部停止 connection/socket generation 上仍在执行的 handler 并丢弃晚到结果。 | `router/src/session/` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-195 | 576bbd31 | doc/reference/static-semantics.md | §1 value semantics（reference 版）：record/object/Array/Map/JsonObject 及包含它们的名义 aggregate 采用 value semantics，赋值/普通传参/返回/container store 创建当时内容的逻辑 snapshot，之后通过任一 writable binding 修改不影响其它 snapshot；实现可用 move/share/COW 避免 deep copy 但不能暴露 physical backing identity 或 mutable alias；`const` 只能在顶层声明是 compiler-evaluated、request-independent、deeply frozen 的值；`let` 与 `var` 只能是局部声明，三者都必须在声明处初始化；局部 let 与普通 parameter 的 binding 取得 logical snapshot 且不可重绑，从它们以及顶层 const、loop/pattern/with binding 派生的 member/index path 都不可写；aggregate writable access-path root 只有三类：局部 var、当前有效的 inout loan、Actor method 中允许直接写入的 self.field member/index path；var 可重绑，前两类的 name/member/index path 可作为 assignment 或 receiver mutation，第三类直接修改 Actor shared state；Actor method 的 DB transaction body 禁止直接或经 callee 写 Actor field；精确 place 可写性是静态事实不依赖 runtime refcount；opaque resource/capability handle 遵守各自 identity/owner/copy/escape 规则。 | `compiler/source` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-196 | 576bbd31 | doc/reference/static-semantics.md | narrowing 与 const 规则：对稳定路径或其前缀赋值会清除该路径及子路径的 narrowing，以该路径作为 inout 参数调用后也执行同样清除；普通赋值/传参/返回/container store 是 logical snapshot，因此普通 callee 不能通过 aggregate 参数修改 caller path，也不因普通调用无条件清除 caller narrowing；identity-bearing resource 与 inout 按各自显式 effect/write path 处理；顶层 const 初始化必须是纯的、可终止的、请求无关常量表达式，不得保存请求上下文/用户/trace/事务/resource-callback capability/临时缓存或随 request 改变的值；Array/Map/record 和 nominal aggregate 可出现在顶层 const 中，前提是初始化图可由 const evaluator 完整求值并 deep freeze；读取该值不产生 request allocation；若程序需要可写 snapshot 必须先存入合法 writable root，首次写入按 value/COW 语义分离 frozen backing。 | `compiler/source` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-197 | 576bbd31 | doc/reference/static-semantics.md | §12 effect metadata 更新：函数 effect metadata 描述 inout、Actor self.field 与 identity-bearing resource 的 read/write path、external effect target 与 conflict-key、resource provenance、boundary 分析需要的 throw payload provenance、callback profile、stream 生产/消费行为和推断的 suspension summary；普通 aggregate 参数与返回值是 logical snapshot 不携带 caller-writable origin；旧 mutable aggregate root/heap-alias 模型以及为它发布的 returnOrigins/directReturnOrigins 不再是 aggregate 正确性事实，也不进入目标 artifact；compiler 可为 move/share/COW 优化跟踪 physical provenance，但不得把它变成用户可观察 alias 或用它决定普通 aggregate 能否进入 service boundary；只有语义上具有身份的 resource/capability handle 保留显式 provenance 与 lifetime descriptor，无法证明 owner/lane safety/boundary eligibility 时 fail closed；inout 参数的读/写集使用参数序号+结构化 selector path 跨 Package artifact 发布，path 只允许稳定字段与 container element projection，未知/非法/超限 fail closed；inout mode、path effect 与 maySuspend 进入 Package Local ABI 但不进入 ServiceProtocolIdentity；`NoPending` 是经验证的 sound maySuspend=false 事实，没有 async 或显式 yield 语法不能单独证明 NoPending。 | `compiler/source/src/callable_effects.rs` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-198 | 576bbd31 | doc/reference/static-semantics.md | §12.2/12.3 writable places/InOut 与 concurrent 规则：普通 parameter 在调用进入时取得 immutable logical snapshot；callee 需修改自己的 aggregate copy 时先写 var local = parameter，该修改不影响 caller；只有显式 inout 参数是 caller-writable loan 且在 callee 中是当前有效的 writable root；规则：inout 只用于 Package-local execution 中的 exact concrete target（同 Package local target 或经 Package Local ABI 精确解析的 package-direct callable），不得通过 interface/dynamic dispatch 猜 target；argument 必须是从局部 var 派生的精确 name/member/index place 并在 call site 写作 inout place，Actor self.field、局部 let、普通 parameter 和顶层 const 都不是合法 actual；compiler 必须证明整个调用期间 loan exclusive（两个重叠 path 不能同时作为 argument，loan 不能被 callback capture、保存到 resource 或让 concurrent sibling 观察）；callee 必须有 verified NoPending，loan 不能跨越 child/host Pending；inout 可进入 Package public API 与 Package Local ABI，但不得进入 service operation/ServiceContract、gateway entry/handler、interface requirement、callback、Actor external method、host effect ABI 或 durable/recoverable payload；写入执行时立即对 caller place 生效，ordinary throw 不回滚已执行写入；verifier 必须重新验证 exact target/parameter mode/exclusive loan/NoPending；concurrent 的 sibling lane 禁止写入从 concurrent 外层捕获的 var 或从外层 inout 参数派生的任何 path（即使静态不同字段），sibling lane 不得发起 inout 调用；lane-local var 仍可在本 lane 内修改；serial 只收束顺序不绕过禁令；callback capture 的读写集并入所属 lane；identity-bearing resource 仍按 effect/conflict-key 检查跨 lane 冲突。 | `compiler` ＋ `runtime/linker` | missing | Phase 2 / 3B | TBD（Phase 0 审计） |
| R-199 | 576bbd31 | doc/reference/static-semantics.md | service boundary 与 recoverable 更新：含 inout 参数的 callable 可以是 Package public API，但只能被 Package-direct 调用，其 service boundary projection 必须是 Unavailable(InOutNotAllowedAtServiceBoundary)；inout 也不允许出现在 external ingress handler/pre/guard、interface requirement、callback 或 Actor external method；inout loan、writable place 与 physical backing identity 不是值，不能进入 recoverable state plan，DB/dispatch/queue/persistent payload 不得保存它们或伪造远程 writeback。 | `compiler` ＋ `runtime/boundary` | missing | Phase 2 / 6B | TBD（Phase 0 审计） |
| R-200 | 576bbd31 | doc/reference/std-surface.md | collections surface（reference 版）：Array/Map 是 aggregate value 不暴露 reference identity；赋值/普通传参/返回/container store 产生 logical snapshot，普通 callee 不会获得 caller-writable origin，Runtime 可用 move/share/COW 共享 physical backing；Array.empty/Map.empty/literal 构造返回普通 collection value；receiver mutation 要求精确 name/member/index writable path，其 root 只允许局部 var、有效 inout loan 或 Actor method 中允许直接写入的 self.field path；前两类修改当前 local/loan，第三类直接修改 Actor shared state；把 Actor field 读入普通 local 或传给普通 parameter 只得到 snapshot；Actor method 的 DB transaction body 禁止直接或经 callee 写 Actor field；let/普通 parameter/顶层 const 派生 path 不可写；顶层 const aggregate 是 deeply frozen，把其 snapshot 存入 verified writable root 后才能修改，shared/frozen backing 在所属 heap 首次写入时按 COW 分离；Array 提供长度读取/push/set/pop/map/filter 基础 surface，push/set/pop 是 receiver mutator 只接受 writable path，map/filter 是 pure transform；Array.concat(left, right) 与 Map.merge(base, updates) 是 type-namespace static transform 返回拼接/合并值不修改输入；Map 提供长度读取/keys/get/has/put/delete，put/delete 是 receiver mutator 其余是 local read；source surface 不定义 appending/setting 等依赖英语词形区分 purity 的成对 API；Map.keys() 返回 key 的 request-local 快照值；for key in map 等价遍历 map.keys()；collection generic 参数不做协变（Map<string,string> 不能整体赋给 Map<string,Json> 或 JsonObject）。 | `compiler` ＋ `runtime/model/src/value.rs` | missing | Phase 6B | TBD（Phase 0 审计） |
| R-201 | 576bbd31 | doc/reference/std-surface.md | config/std.db/WebSocket 更新：`config.require<T>(path)` 表示 required path，overlay 后缺失应导致 deployment publication 失败，运行时 shape 不匹配抛 config.DecodeError；config read 只读取当前 Package slot 由 deployment-owned `BakedConfigPayload` 物化的配置视图，不表示外部 I/O 不产生 external effect 也不改变 package/deployment identity；同一 Package build 在不同 ServiceDeployment 中获得彼此隔离的 ConfigView；`std.db.ConstraintError` 只暴露 kind/声明 DB object 的 Package ID/源码声明的 logical collection identity，不暴露 Mongo 原始消息/物理 collection 名/物理索引名/冲突值；image load 期间创建唯一索引发现已有重复值也使用同一脱敏 constraint 分类拒绝该 build，该控制面失败不进入业务 catch；WebSocket broker 归属更新为 connection/socket-generation，wrong-connection/wrong-socket-generation/未知 id 的 response 不能命中 pending 调用并以协议错误 1002 关闭。 | `runtime/host`（service_db）＋ `router/src/session/` | missing | Phase 3A / 6A | TBD（Phase 0 审计） |
| R-202 | 576bbd31 | doc/reference/syntax.md | 关键字与声明规则：关键字加入 `var`、`inout`；`const` 只是顶层声明，`let` 与 `var` 只允许出现在 block 内，局部 `const` 不是语法，三者都必须在声明处提供 initializer（TopLevelConstDecl / LocalBindingDecl 文法）；三个名称职责不重叠：顶层 const = compiler-evaluated、request-independent、deeply frozen 值；局部 let = 运行时不可重绑且不可通过该 binding 修改的值；局部 var = 可重绑、可作为 writable access-path root 的值。 | `compiler/syntax` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-203 | 576bbd31 | doc/reference/syntax.md | 函数头与赋值：普通参数写作 name: Type，显式 write-through 参数写作 `inout name: Type`（inout 是参数 mode 不是类型修饰符，其 Package-local exact-target 限制由 static semantics §12.2 检查）；赋值是 statement 不是 expression，左侧 place 由名字/member/index 组成，语法不把任意 place 当作可写，static semantics 只接受从局部 var、当前有效 inout loan 或 Actor method 中允许直接写入的 self.field 派生的精确 writable path，Actor DB transaction body 还有额外禁写规则；与 inout 参数对应的 argument 必须显式写作 `inout place`，place 只能是从局部 var 派生的 name/member/index access path，inout argument 不是可单独使用的 expression，Actor self.field 也不因本身可写而成为合法 inout argument。 | `compiler/syntax` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-204 | 576bbd31 | doc/reference/testing.md | 测试服务与部署模型：test-only declaration 只参与测试编译，不进入 production artifact、deployment closure、public API surface 或 config metadata；测试源码属于独立 test service，使用普通 PackageArtifact/ServiceContract/ServiceDeployment 格式，不定义 TestServiceArtifact；test service 的 config profile 固定 skiff-test；同一个 test service 在一次 runner execution 中只编译一次 PackageArtifact，所有 selected cases 共享 authored config layers 和 dependency graph；非 live runner 按发现顺序把 case 装入有界 batch，batch 只是 runner 调度和有界资源回收单位，不是 runtime artifact、admission 或 generation；每个 case 仍有独立 synthetic ServiceDeployment、ServiceContract、gateway entry/ingress binding、deployment-owned BakedConfigPayload、精确 buildId、release pointer、系统派生 service 数据库、heap、effect registry 和 execution nonce；共享 Package artifact 不等于共享 deployment/ConfigView/mutable state。 | `scripts`（test-runner）＋ `compiler` | missing | Phase 3A | TBD（Phase 0 审计） |
| R-205 | 576bbd31 | doc/reference/testing.md | 测试执行与 authority：runner 在开始任何 dispatch 前构建、验证并发布全部 case deployment，再写入各自的隔离 release pointer；Runtime 按 case 的精确 buildId 懒加载 immutable DeploymentExecutionImage，batch 不生成 RuntimeAssembly/RuntimeConfigSnapshot/activation generation，Runtime 也不以 batch/profile/version/current pointer 作为 load key；runner 在对应 generated deployment 的 baked config 中增加 `skiff.test.ingressUrl` 动态只读 overlay，Package 不读取 ambient environment，authored 文件不能覆盖它；每次 root test dispatch 都新建不透明 `testCaseCapability`，与精确 deployment buildId 共同定界、钉住 execution owner，capability 或 buildId 任一项都不能单独授权派生或替代另一项；direct/recursive dispatch、同步 Actor method call 与 dispatch actor.method(...) 都是同一 case 的派生请求，携带父请求同一 capability 和 active parent request id；Router 只从同一 Runtime session 上仍 active 的父请求授权派生；另一个 case 的 root dispatch 即使复用同一 Package artifact 也必须获得不同值；携带 test capability 的 Actor method 首版必须与 active parent 属于同一 service，并在父请求的精确 owner runtime/connection 上执行，fail closed 不承诺跨 service/Runtime 共享 test effects。 | `router/src/task/` ＋ `scripts` | missing | Phase 3A / 7 | TBD（Phase 0 审计） |
| R-206 | 576bbd31 | doc/reference/testing.md | release pointer 推进不重绑已 active 的 test Actor execution：同步 Actor call、Actor dispatch 与 self-ingress 继续使用进入 execution 时已固定的 capability、parent chain 和 deployment buildId，Router 从该 immutable deployment 读取 gateway metadata，不重读 current pointer 也不构造另一份历史 route snapshot；live runner 的 release target（--profile）选择外部 Router 使用的 release pointer namespace，release target 不得反向选择 config.<profile>.yml；production build 要求：生产文件中出现 test block 是编译错误，*.test.skiff 不进入 production source set，test-only code 不进入 production file artifact bytecode 或 deployment closure，test-only config reads 不进入 production config use metadata，test-only declarations 不进入 production package API 或 service protocol surface。 | `scripts`（test-runner）＋ `compiler` | missing | Phase 3A / 8 | TBD（Phase 0 审计） |

小计（576bbd31）：156 行（40 个文件：31 M + 9 D，全部覆盖）。

---

## C. `430e5ff2` — docs(architecture): close bytecode VM contracts

13 个文件（全 M）。按语义单元归并。

| ID | Commit | 文件 | 语义要求 | Owner | 状态 | 目标阶段 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| R-207 | 430e5ff2 | doc/architecture/actor-model.md | 版本选择是逐 Actor identity 的：每次 actor get/create 或方法调用携带 exact deployment `buildId`，该 build 不进入 logical identity，而是该请求希望访问的 Actor 代码版本；不存在 service 级 Actor release pointer，也不要求同一 Actor type 的所有 key 同时切版（`DocHub("a")` 可运行 build A，`DocHub("b")` 同时运行 build B）。 | `router/src/actor/types.rs` ＋ `runtime/host` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-208 | 430e5ff2 | doc/architecture/actor-model.md | 一个 live incarnation 从 create 到销毁始终 pin 同一个 exact buildId、immutable DeploymentExecutionImage、implementation identity 和 owner runtime；其所有已 admission 的 method 与 continuation 都在 owner image 中执行；调用方自己的 image 只负责在调用边界生成/验证参数和结果，不能把 caller image-local type/shape/const/method-table 索引直接带进 ActorStateHeap；请求的 build 与 live incarnation build 不同时不在旧 heap 上执行新代码，直接按拒绝规则处理；运行时不存在 RuntimeAssembly、current assembly、activation generation 或 Actor 专用 release pointer。 | `runtime/eval/src/actor_instance.rs` ＋ `runtime/host/src/host/actor_owner_execution.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-209 | 430e5ff2 | doc/architecture/actor-model.md | `std.actor.get` 是按 logical identity / live owner fence 串行化的 get-or-create：live incarnation 已存在且 build 相同 → 返回现有句柄忽略本次 create 参数；live incarnation 已存在但 build 不同 → 拒绝本次 get，不更新 entry，不影响旧实例的 idle/lease 时钟；没有 live incarnation → 本次 get 竞争新的 owner claim，获胜者把自己的 exact build、ABI 与 create 输入绑定到新 incarnation，旧 entry 中保存的 create 输入只有在 expected plan 与本次 build 精确兼容时才能复用，否则使用本次 get 的完整 create 输入，缺失则 fail closed。 | `router/src/actor/ownership.rs` ＋ `router/src/actor/activation.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-210 | 430e5ff2 | doc/architecture/actor-model.md | 版本 admission 第一版固定，不升级 live Actor、不记录 current/newest build、不比较 semver，六条规则：1) 请求 build 与 live incarnation exact build 相同才 ordinary admission 并刷新 owner lease/idle 时钟；2) build 不同的 get/method/durable task 直接以 `ActorVersionRejectedError` 拒绝，不进入当前 heap、不更新 registry/version facts、不触发 retirement、不刷新旧 incarnation 的 lease/idle 时钟；3) 同 build 已 admission 的 method/continuation 继续由 owner image 执行直到正常结束/失败/普通生命周期回收，发布新 build 本身不打断它们；4) idle TTL、owner runtime 断连、shutdown 等普通生命周期事件销毁 live instance 后释放 owner image pin、清除该 incarnation 的 build admission facts 并推进 epoch，旧内存状态不保存不复制；5) 此后第一个成功取得该 identity owner claim 且携带完整或兼容 create snapshot 的请求用自己的 exact build 重新执行 create，平台不记住哪个 build 是新版，因此允许前进也允许旧 build 重新获胜而回退；6) 两个 build 并发竞争空 identity 时只允许一个 claim/owner fence 成功，失败方重新观察结果并在 build 不同时按第 2 条拒绝。 | `router/src/actor/ownership.rs` ＋ `runtime/host/src/host/actor_owner_execution.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-211 | 430e5ff2 | doc/architecture/actor-model.md | 生命周期与恢复：runtime 根据 idle TTL 自动发起逐出（TTL/扫描周期是 operator/runtime policy 不是业务可依赖的定时承诺）；达到 TTL 后先关闭新 admission 并把 live instance 标为待销毁，已在运行的同步段只在回到 runtime 检查点时观察 fence，suspended continuation 在 resume 前观察 fence，只有 active/suspended/cleanup 计数全部归零后才物理释放 heap；Router 不能仅因 owner lease 本地过期就把实例当成已销毁并开放新 owner（正常 idle 路径要求 exact-owner discard ACK，owner 断连/crash 路径要求 session/incarnation fence 使旧 owner 不再能 admit/resume，新 incarnation 才能发布，否则在 Router 无 owner 而 Runtime 仍有残留 instance 时破坏唯一性）；正常 idle 逐出清理 live 内存和该 incarnation 的 build admission facts，registry 可以保留 key/create 输入作为重建材料但它不是版本指针，下次激活由首个 claimant 的 exact build 决定，保存输入与该 build 的 create plan 不兼容时必须要求该请求提供新的完整 create 输入否则 fail closed；不同 build 的拒绝不改变当前实例。 | `router/src/actor/lease.rs` ＋ `router/src/actor/ownership.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-212 | 430e5ff2 | doc/architecture/actor-shared-heap-design.md | v5 版本边界决策：每 Actor identity 的 live incarnation 钉住创建它的 exact deployment buildId/image，不同 identity 可同时运行不同 build；live 期间不同 build 请求只拒绝，不进入 upgrading、不触发逐出、不刷新 idle，实例由普通 idle TTL/断连/shutdown 回收；销毁后不保留 newest/superseded pointer，下一次 owner claim 由请求自己的 build 决定允许回退；第一版不做 Actor ABI 兼容复用，旧 upgrade 状态机/控制帧不属于目标态。 | `runtime/eval/src/actor_*` ＋ `router/src/actor` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-213 | 430e5ff2 | doc/architecture/actor-shared-heap-design.md | quiescence 压缩与旧状态机：实例维护 active/suspended 续体计数（含 create 和 transaction cleanup）与 discard 状态，不再有 upgrade 状态；触发条件计数==0、无 pending discard、arena 超阈值、实例仍存活；逐出优先级高于压缩，实例进入 discard 时跳过压缩，不同 build 请求只拒绝不改变压缩状态；旧 router idle 逐出与 upgrade 互卡状态机直接删除，不以修复 upgradeFence 的方式保留第二套版本切换；回归测试改为证明 cross-build mismatch 不触发 control frame、不刷新 idle、idle ACK 只影响 exact owner，并在 Runtime exact discard 确认后推进 incarnation epoch；当前默认 owner lease TTL 与 idle TTL 都为 30s 且 sweep 先 expire owner 会跳过 IdleEvict，落地时必须调整时序/所有权协议，并端到端证明 Router 释放 owner 时 Runtime store 不留可重用的残留 instance。 | `runtime/eval/src/actor_executor.rs` ＋ `router/src/actor/lease.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-214 | 430e5ff2 | doc/architecture/actor-shared-heap-design.md | 跨 runtime 边界（v5）：每次 Actor invocation 携带 exact deployment buildId，owner Runtime 按该 key 取得/懒加载 immutable DeploymentExecutionImage 并在 method entry/resume 校验 exact image owner 与 ActorImplementationIdentity；Actor shared arena 属于该 incarnation 并由同一个 image 解释，不得用另一 build 的 image-local type/shape/const/behavior 索引访问它，也不经 RuntimeAssembly 或 activation generation 切换；cross-build invocation 直接 ActorVersionRejectedError，不刷新 idle 或触发升级；逐出/断连后字段不跨任期，下一个成功 claimant 按自己的 build 与 create snapshot 重建允许回退；验收矩阵新增：不同 identity 异 build 并存、同 identity mismatch 只拒绝且不刷新 idle、idle/断连后任意 build 重新 claim。 | `runtime/eval/src/actor_instance.rs` ＋ `runtime/host` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-215 | 430e5ff2 | doc/architecture/any-interface-value.md | 当前实现边界：必须区分三条形似回调的路径，不能互相当作完成证据：1) Package-local `any I`（Agine 把本地 LLM/event adapter 传入 Agent package，adapter 内部再发普通 Agine→AIHub service call）始终是 Local method-table dispatch 加一条正向 service operation，不是 callback capability，也没有把 any I 送过 Router；2) Service operation 顶层 any I 的 boundary projection：当前 Runtime 已能在同一 runtime/execution assembly 内登记 opaque capability 并在 provider 调用时切回 owner，这是 InProcessBoundary；3) 跨 runtime/Router 的反向 callback：当前 Router wire 没有 callback request/cancel/response family，capability 也不能经 JSON/recoverable/DB codec，RemoteBoundary 在新增 owner route、认证、lifetime、cancellation、deadline 与 backpressure 协议前必须 fail closed，它不是 bytecode VM 单独能补齐的能力。 | `runtime/boundary` ＋ `router` | existing-needs-proof | Phase 6A | 文档断言：any-interface-value.md 当前实现边界第 2 点（同 runtime capability table 已实现）；最终证明 TBD（Phase 0 审计） |
| R-216 | 430e5ff2 | doc/architecture/any-interface-value.md | carrier 收敛为三分支：`InterfaceCarrier` 增加 `CallbackCapability { owner_deployment_build_id, owner_runtime_route, request_identity, operations, opaque_capability_id, lifetime }`（只由 service boundary 的显式顶层 projection 产生，记录 exact owner、允许的 operation 与 request/stream lifetime；同 runtime 调用查 capability table，remote route 只有 RemoteBoundary transport capability 存在时才可执行）；plan（artifact 侧）带 _plan 后缀、linked overlay id（runtime 侧）无后缀；本地必有 payload、remote/callback 必无本地 payload 由 enum 分支天然保证；verifier 不需要再单独对账三轴配对；service op 顶层 any I 可同 runtime 投影成 CallbackCapability，但 Remote carrier 的方法表第一版必须保持 placement-independent，在 RemoteBoundary callback transport 缺失时不能把这类方法投影成可跨 runtime 的 ContractOperationId。 | `artifact-model` ＋ `runtime/boundary/src/value.rs` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-217 | 430e5ff2 | doc/architecture/any-interface-value.md | 调用 lowering 三分支：三种 carrier 的调用 lowering 选 slot 逻辑相同，只有最终 target 解析按 carrier 分支分流；CallbackCapability 分支校验 request/stream lifetime、owner build/route 与 operation slot，同 runtime 通过 capability table 切回 owner execution context，跨 runtime 只有 transport 明确声明 reverse-callback capability 时才可发起否则稳定返回 `CapabilityUnavailable`，不能退化成本地 method table 或普通正向 service call；Remote 分支第一版的 operation table 因 placement-independent 约束不会含返回 any J 的 slot；Local 与 CallbackCapability 分支可按各自 owner/boundary plan 返回；三分支静态 maySuspend 都保守 true 且不会仅因保守 summary 自动 yield；Agine 的 package-local any I adapter 内部调用 AIHub 是 Local branch + 普通正向 service call，不覆盖跨 runtime callback transport 门禁。 | `runtime/vm` ＋ `runtime/boundary` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-218 | 430e5ff2 | doc/architecture/bytecode-vm.md | §1/§2 monomorphization 决策：PackageArtifact 可以保存 generic bytecode template，deployment linker 对 exact package closure 做有限、确定性的 monomorphization；`LinkedBytecodeImage` 中所有 function/frame/type/shape 都已 concrete，不存在 runtime `TypeParam` substitution 或按调用现场临时编译；`DeploymentExecutionImage` 增加 `concreteSpecializations`；`RelocatableBytecodeFunction` 增加 `typeParameters`；deployment linker 职责更新为计算有界 generic specialization closure 并解析 relocation。 | `runtime/linker/src/type_params.rs` ＋ `runtime/linked-program/src/type_params.rs` | missing | Phase 3B | TBD（Phase 0 审计） |
| R-219 | 430e5ff2 | doc/architecture/bytecode-vm.md | §3.3 deployment-link monomorphization 算法契约：泛型采用方案 A（artifact 保留 relocatable template，exact deployment 在 link 时单态化）；SpecializationKey = templateFunctionKey + canonicalConcreteTypeArguments + concreteReceiver/SelfInstantiation；roots = operation/gateway/Actor/package-direct/frozen-const behavior entries，worklist 按 canonical identity order 展开，每 key intern 为 concrete function index 并代换 frame slots/value-transfer plans/type-shape refs/call relocations/return-exception plans/callback captures，按 (call-site pc, key) 顺序入队新可达 specialization；key 的所有 type argument 必须 fully concrete，代换后残留 TypeParam/unresolved associated fact/caller-local inference variable 则 build 立即 link 失败；普通递归复用同一 key（direct/mutual recursion 可指向正在构造的 index），polymorphic recursion 持续生成新 key 或 specialization 数量/单函数-总 code words/type depth 超出受信上限时整个 buildId 以稳定 link error 失败，不得 lazy specialize、截断后 fallback 或把 template 交给 runtime 解释；canonical root/worklist 顺序与 intern 规则保证相同 validated 输入生成相同 linked overlay，并发 load 共享同一 per-buildId 结果；specialization 重算 concrete CallableEffectSummary、frame layout、max operand depth、exception/callback plan 与 tail-call eligibility，不能把 template summary 不经代换直接当作 proof；post-link verifier 拒绝任何残留 TypeParam，VM frame 不携带 generic environment，call_local/tail_call_local 只引用 exact concrete function index；service operation 和 external ingress 第一版仍不能是 generic declaration，可以使用 fully instantiated 的 generic platform/user types。 | `runtime/linker/src/type_params.rs` ＋ `runtime/linker/src/linker.rs` | missing | Phase 3B | TBD（Phase 0 审计） |
| R-220 | 430e5ff2 | doc/architecture/bytecode-vm.md | §3.5 指令族与 §6.5 ValueTransferPlan：新增 Stream 族 `stream_next`/`emit_stream`（stream_next 消费一次性 stream endpoint 的下一项；emit_stream 只能出现在 verified server-stream producer，并在 buffer 满/consumer 未就绪时形成真实 backpressure Pending）；`copy_slot`/`dup`/container store/普通 by-value argument preparation 按 linked `ValueTransferPlan` 执行（ordinary aggregate 才做 semantic share transition，不能只复制 16 bytes 留下两个唯一 edit token，也不能复制 move-only/affine resource）；每 linked type/slot/parameter/result/container position 有显式 ValueTransferPlan（SnapshotShare/MoveOnly/AffineResource/ExplicitCloneLease）；`ResourceRef` 不是 ordinary aggregate 不能自动走 share transition；`Stream<T>` endpoint 是 AffineResource/one-shot consumer（赋值/参数准备/return 默认 move，copy_slot/dup/普通 container store/第二次迭代由 source checker/verifier 拒绝）；socket/file/timer 是否 move-only 或可显式 clone 由各自 native contract 决定不能从 slot kind 猜测；move_slot 原子转移 resource token 并清空 source；drop/slot overwrite/frame truncation/tail replacement/normal return/throw-unwind/request stop 都执行 linked drop plan；ResourceTable 以 (resourceId, generation, owner) 做 exact 幂等 release，显式 close 后后续 drop 为 no-op；含 resource 的特权 native record 必须有递归 drop/transfer plan；普通用户 record/schema 仍不能藏 resource；GC 只追踪 resource payload 显式登记的 managed roots 不承担 close 时机；verifier 增加：每 slot/parameter/result/container field 的 ValueTransferPlan 完整，move-only/affine resource 不会经过 copy/dup/普通 snapshot store/多 consumer stream 路径，所有 overwrite/frame-pop/unwind edge 都有 exact drop。 | `artifact-model/src/bytecode.rs` ＋ `runtime/model/src/resource.rs` ＋ `runtime/linker` | missing | Phase 1 / 6B | TBD（Phase 0 审计） |
| R-221 | 430e5ff2 | doc/architecture/bytecode-vm.md | §11 scheduler 扩展：VmControl 增加 `EnterAdapter(AdapterInvocation)` 与 `EmitStream(StreamItem)`；EffectStart 增加 EnterAdapter；BoundaryStart 增加 OpenStreamChild(StreamInvocation)；VmFiber 与 resumable `NativeAdapterFrame` 都是 scheduler unit，两者只能通过上述 control result 交还执行权，不能从 adapter 递归调用 VM，也不能为方便把同步 callback/stream 步骤伪造成 Pending。 | `runtime/vm` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-222 | 430e5ff2 | doc/architecture/bytecode-vm.md | §11.3 resumable native adapters：接收 restricted callback 的 native API（如 Array.map/filter 类 adapter）不能在 Rust 栈上直接 poll callback bytecode；`EnterAdapter` 安装 NativeAdapterFrame（adapterIndex/stateId+bounded adapterState/rootedInputs-partialOutput/callbackClosureRefs/callerDestination+resultPlan/sourceSite），AdapterControl 返回 Continue/EnterChild(CallbackInvocation)/Complete(Result)/Park；adapter 每次需要调用 callback 时返回 EnterChild，child 结果由 scheduler 写回 adapter resume slot 后 adapter 继续迭代；adapter state 只保存 stable handles/owned host bytes 并计入 request budget；callback throw 在原 callback/call site 进入同一 unwind；只有 adapter 自己的 host operation 实际等待才 Park；使大 collection callback、callback 内 local/service call 及真实 Pending 都保持 flat native stack。 | `runtime/native/src/callback_adapter.rs` ＋ `runtime/vm` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-223 | 430e5ff2 | doc/architecture/bytecode-vm.md | §11.4 stream producer/consumer：`OpenStreamChild` 为 verified server-stream invocation 建立 StreamSupervisor、consumer endpoint 与 provider producer fiber；service call 在 provider admission 和 boundary plans 验证完成后先把 affine consumer handle materialize 给 caller 再 poll producer body，不能先同步跑 producer 直到 buffer 满才让 caller 取得 handle；gateway root 把同一 supervisor 接到已有 external response sink；producer 始终 pin exact provider build、request frame/deadline/trace/call stack 与 provider heap，不是 detached coroutine，不允许普通 local function 任意 spawn 可逃逸 stream；producer 执行 emit_stream 时返回 EmitStream，supervisor 只在 waiting consumer 或 bounded buffer 有容量时同步接收，否则把 producer 停在真实 backpressure PendingOperation；stream_next 消费一个 item/end/error，item 按 boundary plan materialize 不能共享 provider heap handle；producer normal exit 生成一次 end，throw/timeout 生成一次 error；consumer break/drop/ancestor stop 关闭 affine endpoint 并向 producer 发送 best-effort stop，晚到 item 只做内部清理；同一 endpoint 不得被两个 fiber/lane 消费；native external source stream 也登记在同一 ResourceTable/Supervisor contract 但 producer 由 host adapter 拥有；Package-direct stream wrapper 复用当前 request 已有的 stream registry，普通 Skiff package/local body 仍不能创建新的可逃逸 Stream。 | `runtime/boundary/src/stream.rs` ＋ `runtime/vm` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-224 | 430e5ff2 | doc/architecture/bytecode-vm.md | §11.5 pending completion cell：Start/completion/deadline/internal stop/cancel 可能发生在不同线程，Park 不能依赖先登记 waiter 后端才可能完成的时序假设；每 pending site 使用受信 completion cell（PendingCellState：Open(rootEscrow)/Waiting(waiter,pendingOwner)/Settled(outcome,rootEscrow)/Claimed）；adapter 在把 completion handle 交给 host 前先建立 root escrow；host completion 从 Open→Settled 或 Waiting→Claimed 并 enqueue 一次，scheduler 发布 waiting owner 时从 Open→Waiting 或观察 Settled 后→Claimed 并立即 enqueue；deadline/stop/cancel 通过同一 terminal arbiter 竞争 settlement 只有一个 winner，duplicate/late completion 只命中 bounded tombstone 并释放自身 host payload，不能二次 wake/二次 drop/覆盖 terminal；roots、pending budget reservation、callback/resource lease 与 boundary buffers 在 Ready、Pending publication 与 pre-completed outcome 三条路径上必须恰好转移一次；Actor segment lease 只能在 pending owner 及其 roots 已经成功发布后释放，若 publication 失败仍由当前 fiber 同步 unwind；该原子握手必须有 completion-before-register、register-before-completion、deadline/cancel 四向竞态和 duplicate completion 测试。 | `runtime/vm` ＋ `runtime/request/src/execution_budget.rs` | missing | Phase 5 | TBD（Phase 0 审计） |
| R-225 | 430e5ff2 | doc/architecture/bytecode-vm.md | §13.2 UnwindState 扩展：`UnwindState` 字段改为 reason/candidateOutcome(Option Return|Throw|ChildFailure)/selectedOutcome(Option Return|Throw|VmFailure|PlatformTerminal)/responsePublication(NotEligible|Unpublished|Published)/cleanupFailure(Option VmError)/regionCursor/cleanupPhase；规则：normal return/ordinary throw 在必须完成的 commit/abort/close 前只进入 candidateOutcome，cleanup 改变最终语义时不能先向 caller 发布 success；selectedOutcome 一旦选择不被 resume/re-poll/late cleanup 覆盖；只有逃出 request/gateway root 的 outcome 才进入 response publication 且只能 Unpublished→Published 一次；normal transaction exit 先 commit，commit 成功后才选择 return，commit 失败以 commit error 选择 failure 再按 policy 尽力 abort，abort failure 只进 cleanupFailure/telemetry 不覆盖 commit error；ordinary throw 在发布前等待语义要求的 abort 并保留原 throw，abort 失败不把同一 request 变成第二个 terminal；timeout 立即选择当前 timeout scope 的 TimeoutError 仍可由外层 Skiff catch 处理，request deadline 逃出 root 时才投影为 response terminal；internal stop 直接选择不可捕获的 platform terminal；两者 best-effort cleanup 可在 bounded owner 中继续但不延迟已固定语义结果也不能把 late result 写回已结束的 scope/request heap；timeout/internal stop 选择 outcome 后若 cleanup 仍可能 Pending，scheduler 只可把 driver-owned transaction/resource/session 句柄和 owned host bytes 转移给 CleanupOwner（独立有限 budget/deadline 与 pending handshake，不能保留 Skiff frame/request-Actor heap handle/callback closure/用户 bytecode 执行权，不能发起新用户语义 effect/response 写回）；request heap 只有在所有 roots 已释放或原子转移后才能销毁；局部 timeout 被 caught 时外层 fiber 继续拥有原 request heap；普通 return/throw 路径仍由原 request owner 完成语义必要 cleanup，不能为了提早响应把 commit/abort 随意 detach。 | `runtime/vm`（unwind）＋ `runtime/request` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-226 | 430e5ff2 | doc/architecture/bytecode-vm.md | §14 Actor exact-build（v5 版）：每 identity 的 live incarnation 钉住创建它的 exact deployment buildId、DeploymentExecutionImage 与 ActorStateHeap，不同 identity 可同时运行不同 build；method entry acquire ActorSegmentLease 并校验请求 build 与 owner build 精确相等、actor identity/implementation identity、incarnation fence 和 arena epoch；不同 build 的 get/method/task 在 live incarnation 存在时直接 ActorVersionRejectedError：VM/Router 不在旧 heap 上跑新 image 代码、不触发 upgrade/retirement、不刷新旧 owner 的 idle 时钟；普通 idle TTL/disconnect/shutdown 销毁实例后下一个成功 claimant 以自己的 exact build 创建允许回退；不存在 Actor release pointer 或 newest/superseded 集合；第一版即使 ActorAbiIdentity 相同也不跨 build 共享 heap，未来若以 ABI 兼容优化必须先定义所有 image-local type/shape/const/behavior 引用的重绑定 proof，不能只比较一个 hash 便混用两个 image。 | `runtime/eval/src/actor_instance.rs` ＋ `router/src/actor` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-227 | 430e5ff2 | doc/architecture/bytecode-vm.md | §16.1 unbypassable hard fuel：VM dispatch 对每条 semantic instruction 从受信 raw-op quantum 扣 1；quantum 为 0 时无条件 poll deadline/internal stop，并从 execution policy 拥有的 finite instruction limit 扣除已执行数量；总 limit 耗尽立即产生结构化 resource terminal；继续执行时只能由 VM 按固定、非零、受信上限补充下一个 quantum；artifact 不能设置、关闭、跳过或重置 quantum/总 limit，也不能用 metadata 扩大预算；post-link verifier 另证明每个 CFG cycle 经过 budget_checkpoint 以保持语言级 loop/backedge semantic charging 与 source attribution，checkpoint 本身有固定非零 raw-op 成本但不是唯一 interrupt poll；raw hard fuel 界定两次 stop poll 之间的最大工作量，semantic checkpoint 保持既有可观察 budget 单位；decoded fusion/JIT 必须按所含 semantic op 数量扣 raw fuel；任何 runtime micro-op 内部循环也按固定 quantum poll，不能一次调用吞掉无界工作。 | `runtime/vm` | missing | Phase 4 | TBD（Phase 0 审计） |
| R-228 | 430e5ff2 | doc/architecture/bytecode-vm.md | §16.2 semantic charging：emitter metadata 只声明 canonical charge kind/source attribution，数量与合法 pc 由 schema 规则决定，post-link verifier 从 CFG/opcode 重算并拒绝缺失、重复或伪造 entry；VM 可批量提交 unit，但 quickening/micro-op 数/machine instruction 数不能改变语义 unit 也不能影响 raw hard fuel。 | `compiler/emission` ＋ `runtime/linker` | missing | Phase 2 / 3B | TBD（Phase 0 审计） |
| R-229 | 430e5ff2 | doc/architecture/bytecode-vm.md | §18 completion contract 更新：generic Package template 在 deployment link 形成 finite deterministic concrete specialization closure，linked image 无 TypeParam 或 runtime generic environment，polymorphic recursion/上限超出稳定 fail closed；service/Actor/interface/callback 的 EnterChild/Ready/Pending 矩阵、resumable native adapter 和 stream producer/consumer 共享一个 scheduler trampoline，adapter 不得递归 poll VM，emit_stream 具有真实 backpressure；restricted callback synthetic body/capture 和三分支 call_interface 有真实 source-to-runtime test，同 runtime callback capability 执行与 cross-runtime placement fail-closed 分别验收，Agine package-local callback + forward AIHub call 不算 remote callback transport 证据；move-only/affine ResourceRef（特别是 one-shot Stream）在 copy/dup/store、frame pop、tail replacement 与 unwind 上通过 linked transfer/drop plan 验证，release exact 且幂等；Pending completion-before-register、register-before-completion、deadline/cancel/stop 与 duplicate completion 竞态证明单 winner、单 wake、单 root transfer；selected outcome/response publication 唯一，bounded CleanupOwner 不保留结束 heap 或产生 late write；Actor owner fence 包含 exact build，不同 identity 可异版本并存、同 identity mismatch 只拒绝且不刷新 idle、idle 销毁后任意 build 可重新 claim；当前 registry 的 implementation pin 必须从 no-live-owner 路径移除；当前 owner lease/idle TTL 同为 30s 且 sweep 先 expire lease 的默认路径必须修正，不能在未向 Runtime 发送/确认 discard 时只清 Router fence 并把残留 instance 当成已销毁；idle/disconnect 后的新 owner 还必须推进 incarnation epoch 不能让旧 ref/continuation 命中新 instance。 | 全局（所有 §6 区域） | missing | Phase 8 | TBD（Phase 0 审计） |
| R-230 | 430e5ff2 | doc/architecture/compiler-package-pipeline.md | generic source facts：source/type checker 证明每个 generic call 的 type argument 与 constraint，generic body 内可引用词法 TypeParam；lowering 把 generic body 保留为 relocatable template 并在 call relocation 上写 canonical type arguments；Package compile 不要求预见所有 downstream consumer instantiation，也不按本包当前调用点提前复制多份 body；LoweredPackage 增加 generic executable template facts + symbolic instantiation relocations；Package projection 输出 relocatable generic bytecode templates、携带 canonical type-argument expression 的 symbolic instantiation relocations 与 template effect facts；generic call relocation 保留 source 已解析的 canonical type arguments，deployment link 后无 TypeParam 残留，specialization closure 超限/不收敛稳定 fail closed。 | `compiler/source` ＋ `compiler/lowering` ＋ `compiler/emission` | missing | Phase 2 | TBD（Phase 0 审计） |
| R-231 | 430e5ff2 | doc/architecture/compiler-package-pipeline.md | deployment linker 消费 exact package closure 后以 `(templateFunctionKey, canonicalConcreteTypeArguments, concreteReceiver/Self)` 为 key 做有界、确定性 monomorphization（由 bytecode-vm.md 拥有）；普通递归复用同一 specialization key；持续生成新 type argument 的 polymorphic recursion、非 concrete substitution 或超出 specialization/code-size/type-depth 上限使整个 buildId link 失败；VM 不接收 generic environment，也不在请求热路径 lazy specialize。 | `runtime/linker` | missing | Phase 3B | TBD（Phase 0 审计） |
| R-232 | 430e5ff2 | doc/architecture/durable-task-dispatch.md | exact buildId 是 live Actor 版本 admission key；ActorImplementationIdentity 在该 build 内校验 actor declaration/method target，不能替代 build 相等性；get-or-activate 分支改为：1) live incarnation 的 exact deployment buildId 与 task build 相同 → 普通 admission 排队并在 owner pin 的同一个 immutable image 中执行 method；2) live incarnation build 不同 → 直接拒绝 task，不得把 task payload 交给 live owner 的代码，也不触发 Actor 升级、逐出或 idle/lease 刷新，该 attempt 以 ActorVersionRejectedError 明确结束 task terminal 为 platform-failed；3) 没有 live incarnation → task 用自己的 exact build 与 ActorActivationSnapshot 参与 owner claim，成功者执行 create 后再调用 method，registry 中 expected plan 精确兼容的 create input 可以复用但历史 implementation/build 不是版本 pointer；4) Router 重启使 registry entry 丢失时仍用同一 snapshot 恢复最小 entry，多个 build/不同 snapshot 并发竞争同一 ActorIdentity 时只允许一个 claim 成功。 | `router/src/task/actor_attempt.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-233 | 430e5ff2 | doc/architecture/durable-task-dispatch.md | Actor runtime 不记录 newest/superseded build 也不比较 semver；因 idle TTL、owner crash 或 shutdown 已经没有 live incarnation 后，旧 task 的 exact build 可以重新获胜，这是允许的回退不是把 live Actor 反向升级；task lease 不提供第二套版本顺序也不能绕过唯一 live incarnation fence；actor-method target 同步段由 ActorSegmentLease 串行，只有 actual Pending 释放后才与其它协程交错，并继续遵守 exact-build admission 与恢复契约。 | `router/src/task/` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-234 | 430e5ff2 | doc/architecture/package-service-contract-deployment.md | InProcessBoundary 用 runtime capability table 实现，且是当前唯一可执行的 service callback binding；RemoteBoundary 不能复用普通正向 service request：它需要新增 opaque owner route、反向 request/cancel/response、认证、deadline、lifetime 与 backpressure transport；当前 Router wire 没有该 family，因此任何会把 callback 放到另一 runtime 的 deployment 都必须在 admission 时 fail closed，不能以 Agine/AIHub 的普通正向 service call 或 package-local any I 作为完成证据；未来 RemoteBoundary 必须保持与 in-process 相同的语言层 lifetime 与失效语义。 | `runtime/boundary` ＋ `router` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-235 | 430e5ff2 | doc/architecture/package-service-contract-deployment.md | 非目标确认：不实现 RemoteBoundary、service 级进程隔离或独立扩缩容，因此本版 service callback 只允许 InProcessBoundary，placement 不能把 capability owner/provider 分到不同 runtime。 | `runtime/boundary` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-236 | 430e5ff2 | doc/architecture/router-rust.md | ActorRoutingState 的 owner fence 另钉住创建它的 exact deployment `buildId`：actor identity/incarnation/exact-build owner fence/lease、instance get/create dedup、method 与 owner-control correlation、idle/expiry schedule；明确不拥有 release pointer、newest/superseded build；不同 identity 可同时运行不同 build，同 identity 的不同 build 请求直接拒绝不触发升级/逐出也不刷新当前 owner 的 idle 时钟；实例因 idle/断连/shutdown 销毁后下一次 claim 由请求自己的 build 决定允许回退。 | `router/src/actor/ownership.rs` ＋ `router/src/actor/lease.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-237 | 430e5ff2 | doc/architecture/router-rust.md | Router verification 更新：Actor claim/install/release 只由 ActorRoutingState 改变 truth；同 identity 跨 build 拒绝、不同 identity 异版本并存、mismatch 不刷新 idle、idle 销毁后任意 build 重新 claim 都有竞态测试；owner lease expiry 不能抢在 idle discard 之前只清 Router fence，测试必须同时证明 Runtime instance 已被 exact discard 或旧 session 已被 fence；Router 不依赖 VM/eval crate 也不持有 deployment execution image 或跨 service 协调状态。 | `router/src/actor/` ＋ `router/src/supervisor/actor.rs` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-238 | 430e5ff2 | doc/architecture/tail-call-execution.md | generic edge 在 deployment link 时已经按 bytecode-vm.md 单态化，VM 看到的是 exact concrete specialization，不是携带 runtime substitution environment 的 template frame；deployment linker 先解析 exact concrete specialization，post-link verifier 再证明 exact-local target、return plan、concrete self、NoPending/InOut 与 cleanup-region eligibility；迁移期 tree evaluator 仍必须在 caller env 销毁前通过既有 call_type_substitutions 实例化 generic type args，bytecode 完成态不保留这一步（frame 只保存 concrete function index）；tail_call_local 的 verifier 证明要求 caller/callee plan 均无残留 TypeParam。 | `runtime/linker` ＋ `runtime/vm` | missing | Phase 3B / 4 | TBD（Phase 0 审计） |
| R-239 | 430e5ff2 | doc/architecture/tail-call-execution.md | runtime positive 覆盖 generic/impl self 的 concrete specialization；carrier/error 等价验证改为 concrete generic/self specialization 下的 nominal/union/representation/container carrier 等价。 | `runtime/vm` ＋ test | missing | Phase 4 / 8 | TBD（Phase 0 审计） |
| R-240 | 430e5ff2 | doc/reference/any-interface.md | 值布局更新为三 carrier：any I = interface_id + carrier（互斥 enum：Local{concrete_type, method_table, payload} / Remote{dependency, public_instance, operations} / CallbackCapability{owner, request_lifetime, operations, capability_id}）；本地装箱值 fat 值（数据+方法表），远程装箱值只携带寻址坐标与 operation 寻址，callback capability 只携带 opaque owner 与受限 operation；本地必有 payload、远程/callback 必无本地 payload 由 carrier 单个 enum 分支天然保证；显式 as I 装箱产生 Local/Remote 分支，CallbackCapability 只由 service boundary projection 产生；与单态化泛型的边界表更新为 callback 经 owner capability 分派；当前可执行实现只覆盖同 runtime 的 InProcessBoundary capability table，跨 runtime/Router 的反向 callback 需要独立 transport，在它落地前 deployment 必须拒绝把 callback owner 与 provider 分开；Package-local any I 的方法内部再发普通 service call（Agine adapter 调 AIHub）仍只是本地 dynamic dispatch + 正向 service call，不等于 callback 跨过 service/Router boundary。 | `runtime/boundary` ＋ `compiler` | missing | Phase 6A | TBD（Phase 0 审计） |
| R-241 | 430e5ff2 | doc/reference/dispatch.md | actor-method target 的 get-or-activate 分支更新：live build 相同则 admission；live build 不同则 ActorVersionRejectedError → platform-failed 且不改变该 Actor、不触发升级或逐出；没有 live owner 则用 ActorActivationSnapshot 参与唯一 owner claim，允许旧 build 在自然销毁后重新获胜；router 重启后 task 用 exact buildId 参与 owner claim 并懒加载 image，多个 build 竞争仍只有一个 live owner，不依赖 ambient release pointer 或任何 deployment generation。 | `router/src/task/` | missing | Phase 7 | TBD（Phase 0 审计） |
| R-242 | 430e5ff2 | doc/reference/runtime.md | `Stream<T>` 明确是 affine one-shot handle：传参/返回只能转移 consumer ownership，不能普通 copy、放进多个 lane 或开始第二次迭代；normal end、error、break、return、timeout 与 request stop 都恰好一次释放 endpoint 并传播 best-effort source stop；尾调用 eligibility 的 caller/callee return plan 等价判定在 deployment link 形成的 concrete generic specialization 下进行。 | `runtime/model` ＋ `runtime/vm` | missing | Phase 5 / 6B | TBD（Phase 0 审计） |
| R-243 | 430e5ff2 | doc/reference/static-semantics.md | §13.1 generic executable closure：泛型是编译期参数化不是 runtime reified dispatch；source checker 必须为每个 generic call 解析完整、满足约束的 type-argument expression，generic body 内该 expression 可引用词法可见 TypeParam；PackageArtifact 可发布 generic body template，exact deployment linker 再按 (callable, concrete type arguments, concrete receiver/Self) 单态化；同一 concrete instantiation 上的 direct/mutual recursion 合法并复用同一 specialization；polymorphic recursion 不断构造新 type arguments 使 reachable specialization closure 不有限、或对某 concrete key 代换后的 reachable edge 仍含未解 TypeParam 时 deployment link 必须 fail closed；实现可设置受信 specialization 数量/code size/type-depth 上限，超限是 build/link resource error，不允许请求执行时 lazy specialize 或 fallback 到动态泛型解释；该规则不放宽 §14：service-call operation 与 external ingress 第一版仍不能是 generic declaration，只能在签名中使用 fully instantiated 的 generic type；Package public generic callable 可供 package linkage 使用，但只有形成 finite concrete closure 的 exact deployment 才能执行。 | `compiler/source` ＋ `runtime/linker` | missing | Phase 2 / 3B | TBD（Phase 0 审计） |

小计（430e5ff2）：37 行（13 个文件全部覆盖）。

---

## 汇总

| Commit | 文件数 | 行级 `@@` hunk 数 | ledger 语义行数 |
| --- | --- | --- | --- |
| `46351f72` | 1 | 1 | 31（R-001..R-031） |
| `576bbd31` | 40 | 273 | 156（R-051..R-206） |
| `430e5ff2` | 13 | 73 | 37（R-207..R-243） |
| 合计 | 54 | 347 | 224 |

编号规则：按 commit 顺序连续编号（46351f72: R-001..R-050；576bbd31: R-051..R-206；430e5ff2: R-207..R-243），跨 commit 无空洞。状态统计：`missing` 230、`existing-needs-proof` 3（R-152、R-157、R-215）、`retirement-only` 10、`implemented` 0。

说明：`existing-needs-proof` 行（R-152 lazy-load 模型、R-157 tree-evaluator tail trampoline、R-215 同 runtime capability table）的文档断言位置均已注明，最终代码/测试证据由 Phase 0 审计补齐后才可翻 `implemented`。
