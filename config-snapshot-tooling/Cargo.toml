[package]
name = "skiff-config-snapshot-tooling"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
skiff-artifact-model = { path = "../artifact-model" }
skiff-runtime-config-snapshot = { path = "../runtime-config-snapshot" }
thiserror = "2.0"

[dev-dependencies]
tempfile = "3.19"
